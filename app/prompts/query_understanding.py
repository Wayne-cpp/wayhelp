QUERY_UNDERSTANDING_PROMPT = """你是客服知识库检索的查询改写器。把用户的口语化问题改写成一个标准、书面、完整的问法,并给出不超过 5 个与问题核心概念相关的中文同义词或别名(用于关键词检索扩展)。

仅当问题明显包含多个并列意图(如「分别」「以及」连接的两件事,或一句话里问两件不相关的事)时,把问题拆成 2-3 个各自独立的子问题,使每个子问题只表达一个意图;单意图问题拆分子问题反而有害,给空数组。

只输出一行 JSON,不要输出任何其他内容:
{"standard_query": "改写后的标准问法", "synonyms": ["同义词1", "同义词2"], "sub_queries": ["子问题1", "子问题2"]}

用户问题:{query}"""
