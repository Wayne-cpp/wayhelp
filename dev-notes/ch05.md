# ch05 开发实录:LangGraph Workflow 编排骨架 + 主力 Agent

> 本章主题:Workflow 确定性编排做骨架,主力 Agent 作为骨架里的核心节点;全程走 Superpowers 流程。
> 格式约定:每个阶段记四样——我(用户)的关键原话、AI 的关键产出、我拒绝/纠偏了什么、翻车与返工。

## 阶段 1:brainstorm 定稿(2026-09-17)

**我的关键原话**(任务书要点,全文见会话):

- 「用 Superpowers 模式把客服系统升级成生产级架构:Workflow 确定性编排做骨架,主力 Agent 作为骨架里的核心节点」
- 「祛魅热身:先不用框架,手写一遍最裸的 Agent 循环……看清它就是个带工具的循环,再用 LangGraph 重构」
- 「图骨架按这条流程搭:指代消解、意图识别、按意图分流、知识检索、置信度闸、主力 Agent、日志记录」
- 「分流规则写死在代码里」;七类意图→四出口(知识类强制 RAG+置信度闸 / 业务数据类直接进 Agent / 投诉不进 Agent 给两个选项 / 闲聊固定话术)
- 「转人工、建工单是两件事,分开做,都交用户在前端自选、不后端自动执行」
- 「涉及具体库、框架、API 的用法,一律先用 Context7 MCP 查最新官方文档再动手,别凭记忆写」
- 「上面点名的技术选型是定死的,实现中发现矛盾或走不通,停下来问我,不要自行换方案」

**AI 的关键产出**:

- 探索报告:确认现有链路是手写「两次调用」编排(非 ReAct),`RetrievalTrace` 是检索→编排的桥;`create_ticket` 同事务置会话「已转人工」(与「两件事分开」冲突,设计里拆 `write_ticket` 解决);`SERVICE_SYSTEM_PROMPT` 「每轮最多一次工具」条款与 ReAct 多步冲突(设计里修订);langgraph 未装、langchain-core 已 1.6.2。
- Context7 核查(langgraph 1.0.8):StateGraph/条件边/compile(checkpointer)/get_stream_writer/stream_mode/create_react_agent 现行 API 确认可用。
- 5 个澄清问答定案(D1~D5):① 新旧链路**原地替换**;② checkpointer 用 **AsyncSqliteSaver**;③ ReAct **手写循环节点**(不用 create_react_agent 黑盒);④ 建工单走**结构化动作端点** `POST /v1/chat/action`;⑤ 热身裸循环**留档** `examples/bare_agent.py`。
- 设计 spec:`docs/superpowers/specs/2026-09-17-ch05-langgraph-workflow-design.md`(15 节,含分流写死表、State schema、ReAct 停止条件与 token 预算、SSE suggest_actions 帧、验收→测试映射)。

**我拒绝/纠偏了什么**:无。5 个分支问答用户均采纳推荐项,8 节设计一次通过(「通过,写 spec」)。

**翻车与返工**:无。探索阶段发现的两处冲突(create_ticket 置状态、prompt 条款)在进入设计前已被识别并纳入方案,未造成返工。

## 阶段 2:spec 评审与修订(2026-09-18)

**我的关键原话**:「spec我已经使用gpt6进行review并修改了」——评审由我用 GPT-6 完成,改动直接落在 spec 工作区(+159/-61)。

**AI 的关键产出**:本阶段 AI 无新产出物,职责是消化修订、核对内部一致性(工具集/路由表/State 字段/帧顺序/测试映射自洽,与定死选型无冲突)、提交归档。

**我拒绝/纠偏了什么**(本轮是用户方纠偏 AI 原设计,逐条记录):

1. **D4 收紧为「聊天 Agent 无建单权限」**:原设计让 Agent 保留 create_ticket(沿用 ch02 同事务置「已转人工」);修订后 create_ticket 与 query_faq 均退出聊天图绑定与执行注册表,Agent 只有 3 个只读业务工具 + suggest_options 伪工具,伪造 create_ticket 调用只回 unknown_tool;写 tickets 唯一通道是动作端点。
2. **新增 D6 needs_knowledge 分流**:分流键从「七类意图」升级为 `(intent, needs_knowledge)` 14 组合,知识需求优先于意图名称(混合问题、投诉+政策问都先检索);解析失败兜底从原设计的「售后→business fail-open」改为「售后+needs_knowledge=true→knowledge 保守检索」——fail-open 会绕过置信度闸,是原设计真 bug。
3. **检索故障与低置信分离**:NOTE_UNCONFIGURED/REBUILDING/REBUILD_REQUIRED/超时 → `KB_UNAVAILABLE_ANSWER` 不入池;零命中 → REFUSAL_ANSWER 入 `retrieval_low_conf`;高分但模型自评拒答 → 入 `self_check`。原设计把三类混在一个 low_confidence 分支。
4. **State 轮次生命周期表(§10.1)**:LangGraph 同 thread 未覆盖字段会延续,服务必须每轮显式构造全部临时字段初值——原设计只画 State schema 没定重置语义,有串轮污染隐患(evidence/建议按钮/入池标记串到下一轮)。
5. **suggest_actions 帧带 source_message_id**:`commit_turn` 返回本轮用户消息 ID,按钮绑定产生它的那条消息;动作端点必填 source_message_id 并校验归属,杜绝「点旧按钮给新问题建单」;字符串传输防 JS BIGINT 精度损失。原设计「取最近一条用户消息」在多轮后会错配。
6. **token 预算精细化(§7.2/§7.3)**:stream_usage=True 显式开启、调用前预留式预算、usage/estimated/mixed 标记、缺失 usage 不得按 0 计;DeepSeek 端点 stream_options 兼容性列入联调验证项。
7. **消息完整性**:validate_turn 扩展为逐组校验多步工具配对;commit 成功后才发 citations/suggest_actions;失败路径不提交、不发 [DONE]。
8. **验收标准 5→11 条**:新增故障不入池、State 串轮隔离、旧按钮建单正确性、预算前置生效、多步消息整体提交、Agent 零写权限六组验收。
9. 话术定稿:COMPLAINT_REPLY / CHITCHAT_REPLY / FALLBACK_ANSWER 修订,新增 KB_UNAVAILABLE_ANSWER / AGENT_BUDGET_ANSWER。

**翻车与返工**:无代码返工——修订全部发生在写代码之前,正是评审门的价值;原设计三处缺陷(fail-open 绕闸、Agent 持写权限、State 串轮)被拦在实现前。

## 阶段 3:计划评审通过,选定 herdr-duo 执行(2026-09-18)

**我的关键原话**:「[herdr-duo] 根据plan实现计划」——以技能激活作答,选定双人组(master=kimi 编排+评审,slave=claude 执行)落地计划;此前 AI 发出的执行方式二选一提问被我 dismiss,随后用技能指令给出最终选择。

**AI 的关键产出**:

- 实现计划 `docs/superpowers/plans/2026-09-18-ch05-langgraph-workflow.md`(3340 行,15 个任务,commit 8290599),含 Global Constraints(Conventional Commits、每任务 commit 携带 dev-notes 追记、改 prompt/检索后跑全量 pytest、本机 curl 加 `--noproxy '*'`、选型定死不得自换)。
- 计划自审修 5 处:frozen dataclass 改用 `dataclasses.replace`;Task 6 测试改用 db fixtures 播种;剔除 import 冗余;补 test_tool_chat_chain 多组钉;test_a4 清理。
- 搭建 herdr-duo 会话 `dev-wayhelp`(master=kimi / slave=claude,同仓库双窗格),通讯 briefing 已注入,两 agent 均 idle。

**我拒绝/纠偏了什么**:执行方式上否了 AI 默认推荐的 Subagent 驱动(dismiss 提问),改用 herdr-duo——编排/执行分离成两个独立 agent,评审由 master 逐任务做。

**翻车与返工**:无。计划内容本身我未再修改,视为评审通过。

## 任务 1:依赖安装与 LangGraph 冒烟(2026-09-18)

**我的关键原话**:

- 「严格按 Step 1-5 逐步执行,代码块逐字使用,不得自行增删」;验收命令 `uv run pytest tests/test_graph_smoke.py -v` 必须 2 PASS。
- 裁决:「批准你的修复方案:node 改为 return {"tag": state["tag"] or "seen"},两条断言原样保留。这是 plan 代码笔误的修复,不是选型变更。」

**AI 的关键产出**:

- `tests/test_graph_smoke.py` 两用例 2 PASS:InMemorySaver 同 thread 二轮调用(messages 继承 + 二次输入生效)、AsyncSqliteSaver `:memory:` 往返。
- `uv add` 引入 langgraph 1.2.11(满足计划约束 `>=1.0.8,<2`)、langgraph-checkpoint-sqlite 3.1.1(连带 langgraph-checkpoint 4.2.0、aiosqlite 0.22.1 等);langchain-core 保持 1.6.2 未降级,两个停止条件均未触发。
- 计划确认命令系 0.x 旧写法,按 1.x 实况纠偏:langgraph-checkpoint-sqlite 在 1.x 安装进 `langgraph.checkpoint.sqlite` 命名空间(无 `langgraph_checkpoint_sqlite` 顶层模块),`langgraph.__version__` 属性已移除,改用 `importlib.metadata.version` 完成版本确认。
- `.gitignore` 无需改动:已有 `data/` 整目录规则覆盖 `checkpoints.db*`(`git check-ignore` 验证通过)。

**我拒绝/纠偏了什么**(plan 代码笔误,经裁决修复):

- 冒烟测试第 1 用例原样无解:node 无条件 `return {"tag": "seen"}`,第二轮 `ainvoke({"tag": "again"})` 最终态恒为 `"seen"`,断言 `out2["tag"] == "again"` 在任何 langgraph 版本下都不可能通过。slave 用最小实验确证「想验证的机制本身正常」(round2 节点看到的输入确为 "again"、messages 继承 1 条)后停下上报,未自行改测试;master 裁决后 node 改为 `return {"tag": state["tag"] or "seen"}`,两条断言原样保留——修复后反而真实验证了二次输入生效。

**翻车与返工**:

- Step 4 首跑 1 passed / 1 failed(上述断言无解),裁决修复后重跑 2 PASS——返工一轮,未扩散到依赖侧。
- 依赖解析侧无翻车:langgraph 未 <1.0、langchain-core 未降级;唯 websockets 17.1→16.1.1 随装(非约束项,langgraph-sdk 依赖所致)。

## 任务 2:热身留档 examples/bare_agent.py(2026-09-18)

**我的关键原话**:

- 「执行实现计划……Task 2(热身留档 examples/bare_agent.py,祛魅)。先读该文件中 Task 2 一节……严格按 Step 1-5 逐步执行,代码块逐字使用,不得自行增删」;验收:`uv run pytest tests/test_bare_agent.py -v` 必须 3 PASS,`uv run python examples/bare_agent.py` 能打印两轮工具调用与收敛文本。
- 「若发现 plan 步骤走不通或与 spec 矛盾 → 立即停下报告(像 Task 1 那样)……plan 代码笔误类修复也先上报等裁决」。
- 裁决:演示脚本 `ModuleNotFoundError: No module named 'app'` 上报后,选「加 sys.path 引导(推荐)——照 evals/ 既定先例……同一裁决覆盖 Task 3 的 probe_intent.py」。

**AI 的关键产出**:

- `examples/bare_agent.py` + 空 `examples/__init__.py`:不借框架的最裸 Agent 循环(调 LLM → 有 tool_calls 逐个执行并把结果喂回 → 无则收敛;`max_steps` 耗尽回 `BARE_BUDGET_ANSWER`);含自包含演示假模型 `ScriptedModel` 与 `--real` 真模型分支。
- `tests/test_bare_agent.py` 三用例 3 PASS:无工具一步收敛 / 真实工具结果喂回(断言 `"1001" in tool_msgs[0].content`)/ 步数预算兜底。
- 演示脚本输出符合验收:两轮工具调用(query_order → query_logistics,mock JSON 真实喂回)+「收敛于第 3 步:订单 1001 已发货,物流正在派送中。」

**我拒绝/纠偏了什么**(plan 代码笔误,经裁决修复):

- plan 的 bare_agent.py 代码块缺 sys.path 引导:脚本方式运行时 `sys.path[0]` 是 `examples/` 而非仓库根,项目无 build-system、`uv run` 不装项目自身,顶层 `from app.tools.business import MOCK_TOOLS` 必然 ImportError。AI 用 `PYTHONPATH=.` 隔离验证「代码本身正确、纯导入路径问题」并找出仓库既定先例(evals/ 下 5 个脚本均有同款引导)后停下上报,未自行改代码;裁决后照先例加两行(`from pathlib import Path` + `sys.path.insert(0, ...parent.parent)`),plan 其余代码一字不动。同款裁决预先覆盖 Task 3 `evals/probe_intent.py`(同样顶层 import app 且无引导)。

**翻车与返工**:

- Step 4 首跑演示脚本即 ModuleNotFoundError,返工一轮(上报→裁决→加引导→重跑全过);测试侧 TDD 红绿一次过,未翻车。

## 任务 3:Prompts——意图 prompt + 服务话术常量与条款修订(2026-09-18)

**我的关键原话**:

- 「执行实现计划……Task 3(Prompts——意图 prompt + 服务话术常量与条款修订,标注样例验证)。先读该文件中 Task 3 一节……与 Global Constraints 的话术常量段(逐字值)……严格按 Step 1-7 执行。」
- 「SERVICE_SYSTEM_PROMPT 条款修订只动 plan 点名的条款(第 5/7/9 条改写、引用协议段 query_faq 表述改『本轮检索证据』并删『query_faq 每轮最多一次』、新增业务工具数据条款,编号重排),其余条款保留原文;REFUSAL_ANSWER 逐字不变。」
- 「有 BAD 行就调 INTENT_PROMPT 措辞重跑至全中,输出表粘贴进 dev-notes」「发现矛盾/走不通 → 停下报告等裁决,不得自换方案。」

**AI 的关键产出**:

- `app/prompts/intent.py` INTENT_PROMPT:七类意图定义 + needs_knowledge 判定 + `{query}` 占位(渲染用 `.replace` 不用 `.format`,字面 JSON 花括号)。
- `app/prompts/service.py`:新增 COMPLAINT_REPLY/CHITCHAT_REPLY/KB_UNAVAILABLE_ANSWER/AGENT_BUDGET_ANSWER/FALLBACK_ANSWER 五常量(Global Constraints 逐字值);SERVICE_SYSTEM_PROMPT 第 5/7/9 条按 plan 改写(业务数据走工具+政策结论凭证据标角标 / suggest_options 伪工具替代 create_ticket / 允许多步连续调用),引用协议段标题改「本轮检索证据(若提供)列表」并删 query_faq 每轮一次限制,新增第 12 条「业务工具数据可直接作答,无证据不得编造政策规格」,原 12-16 重排为 13-17(内容逐字保留),全文 17 条;REFUSAL_ANSWER 逐字不动。git diff 逐行核对:除点名条款与编号外无任何改动。
- `evals/intent_samples.jsonl` 8 条标注样例(逐字 spec §6.2 验收表)+ `evals/probe_intent.py`(按任务 2 裁决先例加 sys.path 引导;parse 为 Task 8 正式解析器预览版)。
- `tests/test_prompts.py` 全量替换为 4 用例:REFUSAL 逐字嵌入、新契约针([ref_no]/不承诺/suggest_options/不得声称)、五常量值断言、INTENT_PROMPT 形状(含 .replace 渲染钉)。
- 验证:契约测试 4 PASS;probe 真模型 8/8 命中;全量 pytest 372 passed。

probe 判定表(`uv run python evals/probe_intent.py`,终跑):

```text
OK  '订单 1001 的物流到哪了': 期望 ('物流', False) 实得 ('物流', False)
OK  '保温杯还有库存吗': 期望 ('商品咨询', False) 实得 ('商品咨询', False)
OK  '维修寄修流程是什么': 期望 ('售后', True) 实得 ('售后', True)
OK  '未发货订单如何取消': 期望 ('订单', True) 实得 ('订单', True)
OK  '订单 1001 到哪了,退货运费谁承担': 期望 ('物流', True) 实得 ('物流', True)
OK  '我要投诉你们的服务': 期望 ('投诉', False) 实得 ('投诉', False)
OK  '我要投诉,并问一下退货运费规则': 期望 ('投诉', True) 实得 ('投诉', True)
OK  '你好': 期望 ('闲聊', False) 实得 ('闲聊', False)

8/8 命中
```

**我拒绝/纠偏了什么**:无用户纠偏。INTENT_PROMPT 措辞调整系 plan Step 6 预授权路径(「有 BAD 行时调措辞重跑」),未动选型与结构,不构成需上报的方案变更。

**翻车与返工**:

- probe 首跑 6/8:两条混合子问(「订单 1001 到哪了,退货运费谁承担」「我要投诉,并问一下退货运费规则」)intent 均被判成「退款退货」——原 prompt 只定义了单类边界,没定多子问时 intent 归谁。按 spec §6.2「主意图」口径补一条「问题含多个子问时,intent 取主意图(通常是最先提出的那个子问);其余子问只参与 needs_knowledge 判定」后重跑 8/8,返工一轮。
- 契约测试与全量 pytest 一次过(372 passed,含 test_chat_api_tools 的「演示数据/不得承诺」旧针——落在保留的第 8 条上,未受条款修订影响)。

## 任务 4:store 层——CommitTurnResult 与多组工具校验(2026-09-18)

**我的关键原话**:

- 「执行实现计划……Task 4(store 层扩展——CommitTurnResult 与多组工具校验)。先读该文件中 Task 4 一节(约第 509-677 行),严格按 Step 1-6 执行,代码块逐字使用。」
- 「新测试代码块里的 _pair/_turn/InMemorySessionStore/pytest import 若现有测试文件中名称不同或已导入,适配现有写法并在 dev-notes 记一笔;不得为迁就代码块改语义。」
- 「SessionStore 协议、InMemory、DbSessionStore 三处签名同步改 -> CommitTurnResult。」「发现矛盾/走不通 → 停下报告等裁决,不得自换方案。」

**AI 的关键产出**:

- `app/sessions.py`:新增 `@dataclass(frozen=True) CommitTurnResult(source_message_id: str)`;`validate_turn` 中段改写为逐组校验(`close_group()` 在新 assistant 前结清上一组,组内 tool_calls↔tool_ids 一对一;原 while 单组逻辑删除);协议、`InMemorySessionStore.commit_turn` 返回类型同步改 `-> CommitTurnResult`,内存侧 `_msg_seq` 计数器在入池后分配并 `return CommitTurnResult(str(self._msg_seq))`。
- `app/store_db.py`:`_commit_sync` 首部先写本轮 user 行并 `s.flush()` 取自增 ID,其余行照旧追加,同事务 commit 后 `return source_id`(签名 `-> str`);`commit_turn` 改 `-> CommitTurnResult` 包裹 `asyncio.to_thread` 结果。
- 测试适配三处(语义不变):test_sessions.py 顶部既有 import 行并入 `CommitTurnResult`(plan 块的独立 import 行不另起);test_store_db.py 的 `TEST_USER_ID` 按本文件惯例换局部 `USER`(同值 UUID);plan 块内函数级 `import asyncio` 与未使用的 `from sqlalchemy import select` 按既有写法去掉(asyncio 顶部已导入)。其余测试体逐字。
- 验证:`uv run pytest tests/test_sessions.py tests/test_store_db.py -v` → 22 passed 全绿(旧用例全过;`test_commit_rolls_back_on_failure` 的 monkeypatch 在新显式 flush 处引爆,断言不变)。

**我拒绝/纠偏了什么**:无用户纠偏。plan 与现状核对无矛盾,未触发上报。

**翻车与返工**:

- 无。TDD 红绿一次过:Step 2 红(ImportError: CommitTurnResult 不存在,与计划预期一致)→ Step 5 一次全绿。

## 任务 5:write_ticket 抽取与 create_ticket 兼容改造(2026-09-18)

**我的关键原话**:

- 「执行实现计划……Task 5(write_ticket 抽取与 create_ticket 兼容改造)。先读该文件中 Task 5 一节(约第 681-783 行),严格按 Step 1-5 执行,代码块逐字使用。」
- 「write_ticket 是纯写库 helper:只 INSERT + flush,不开 Session、不 commit/rollback;create_ticket 行为不变(工单 + conv.status=已转人工 同事务)。」
- 「测试代码块中 build_tools/TurnToolset 等名称以 app/tools/business.py 现有实际接口为准适配,语义不动。」「发现矛盾/走不通 → 停下报告等裁决,不得自换方案。」
- 「dev-notes/ch05.md 末尾追加『## 任务 5』段记四样……按 Task 5 Step 5 的 commit 命令提交,只提交上述 3 个文件。不跑全量 pytest,不起 uvicorn。」

**AI 的关键产出**:

- `app/tools/business.py`:新增模块级 `write_ticket(db_session, conversation_id, description, ticket_type) -> str`(置于 build_tools 之前,即 create_ticket 定义之前),只 `add(Ticket(...))` + `flush()` 返回 ticket_no(单号生成式 T+UTC 时间戳+token_hex(6) 原样搬入,27 位不变);`create_ticket` 闭包体改为 `ticket_no = write_ticket(s, ...)` → `conv.status = "已转人工"` → 同一 `s.commit()`,删去闭包内原单号生成与 `s.add(Ticket(...))` 两行,对外行为不变(Task 6 端点可直接 import write_ticket)。
- `tests/test_tools.py` 逐字追加 3 用例:helper 插行不碰会话状态(conv.status 仍「进行中」)、调用者上下文回滚后 tickets 零行、create_ticket 兼容行为(工单落库 + 已转人工)。无名称适配——build_tools/TurnToolset/tools_by_name/invoke(args dict) 与现有接口全部对上。
- 验证:`uv run pytest tests/test_tools.py -v` → 12 passed 全绿(旧 create_ticket 用例含 27 位单号断言照过)。

**我拒绝/纠偏了什么**:无用户纠偏。plan 与现状核对无矛盾(Conversation 默认「进行中」、Ticket 默认「待处理」与断言吻合;session factory `expire_on_commit=False` 保证测试里 conv.id 跨 session 可取;同步用例与本套件惯例相容),未触发上报。

**翻车与返工**:

- 无。TDD 红绿一次过:Step 2 红(ImportError: write_ticket 不存在,2 failed 与计划预期一致)→ Step 4 一次 12 passed。

## 任务 6:动作端点 POST /v1/chat/action(2026-09-18)

**我的关键原话**:

- 「执行实现计划……Task 6(动作端点 POST /v1/chat/action,建工单唯一写通道)。先读该文件中 Task 6 一节(约第 787-991 行),严格按 Step 1-5 执行,代码块逐字使用。」
- 「AppRuntime 是 frozen dataclass,测试里一律 dataclasses.replace,不直接赋值(plan 自审记录第 1 条)。」
- 「端点行为:同一 Session 完成归属/消息查询与写入;description 取被绑定 user 消息完整文本;不改 conv.status;任一归属/消息不符 404,参数非法 422;写操作 shield。」
- 「chat_service.py 的 __init__ 只加 session_factory=None 一个参数并存下,其余不动;main.py 只在 ChatService 构造处加 session_factory=runtime.session_factory。」「发现矛盾/走不通 → 停下报告等裁决,不得自换方案。」

**AI 的关键产出**:

- `app/schemas.py`:uuid/session_id 校验逻辑抽为模块级 `_validate_uuid`/`_validate_session_id` 供两模型共用(ChatStreamRequest 两 validator 改为委托调用,行为不变);新增 `_SOURCE_MSG_ID_RE`(`^[1-9]\d{0,18}$`)+ `ChatActionRequest`(action 只容 create_ticket、ticket_type 三值 Literal、source_message_id 十进制串校验)。
- `app/services/chat_service.py`:import 补 `AppError`;`__init__` 仅加 `session_factory=None` 存 `self._session_factory`;新增 `create_ticket_from_action`(`asyncio.to_thread` 包同步写库,`ensure_future` + `shield` 保证取消时事务落地再放行;未配 session_factory 抛 `AppError("action_unavailable")`)与 `_create_ticket_sync`(同一 Session 内 `_as_db_id` 解析 → 会话归属核对 → 消息存在/归属/role=user 三重核对,任一不符统一 `SessionNotFoundError` 不泄露信息;description 取被绑定 user 消息完整文本兜底话术;`write_ticket` 纯插行后单一 `s.commit()`,不碰 conv.status)。
- `app/routers/chat.py`:追加 `POST /v1/chat/action` 路由,取 `app.state.chat_service` 调 service,返回 `{"ticket_no","status":"待处理"}`;404/422 走 main.py 既有异常处理器。
- `app/main.py`:仅 ChatService 构造处加 `session_factory=runtime.session_factory`。
- `tests/test_chat_action.py`:4 用例(db fixtures 播种,不走聊天链路,Task 13 换图驱动免疫)——建单成功且 conv.status 保持「进行中」、消息不在会话内 404、归属不符 404、参数非法 422;AppRuntime 全部 `dataclasses.replace` 注入 session_factory。
- 验证:`uv run pytest tests/test_chat_action.py -v` → 4 passed 全绿。

**我拒绝/纠偏了什么**:无用户纠偏。plan 与现状核对无矛盾(conftest 四名称齐全、`asyncio_mode="auto"`、httpx/ASGITransport 既有先例、`write_ticket`/`_as_db_id`/SessionNotFoundError→404 与 RequestValidationError→422 处理器均就位);三处 plan 隐含的必要补全(schemas 抽模块级函数系 plan 原文明示「抽成模块级函数供两个模型共用」、chat_service 补 `AppError` import、router 补 `ChatActionRequest` import 均为代码块必然要求)不构成方案变更,未触发上报。

**翻车与返工**:

- 无。TDD 红绿一次过:Step 2 红(路由不存在,2 failed / 2 passed——两个期待 404 的用例被路由缺失的 404 顺带满足,属预期红态)→ Step 4 一次 4 passed。
- 一处 plan 文字笔误未引发返工:Step 4 写「Expected: 3 PASS」但其代码块本身含 4 个用例,以代码块为准,4 passed 即验收达成。

## 任务 7:图基建——state.py / errors.py / events.py(2026-09-18)

**我的关键原话**:

- 「执行实现计划……Task 7(图基建——state.py / errors.py / events.py)。先读该文件中 Task 7 一节(约第 995-1196 行),严格按 Step 1-5 执行,代码块逐字使用。」
- 「ChatGraphState 字段逐字按 spec §10.1 表与 plan 代码块;ROUTE_TABLE 14 组合全覆盖,needs_knowledge=True 优先于意图名称;new_turn_state 不含 messages 键。」
- 「发现矛盾/走不通 → 停下报告等裁决,不得自换方案。」「不跑全量 pytest,不起 uvicorn。」

**AI 的关键产出**:

- 新建 `app/graph/` 包(`__init__.py` 空文件);动手前先做 plan↔spec 交叉核对——spec §10.1 表 16 个字段与 plan 代码块逐字段一致(messages 的 add_messages 注解、全部临时字段类型与初值、new_turn_state 缺席 messages 键),ROUTE_TABLE 14 组合(7 意图 × needs_knowledge 2 态,True 全部 → knowledge)与测试 EXPECTED 吻合,无矛盾未触发上报。
- `app/graph/state.py`:`INTENTS`(7 元组)、`ROUTES`(4 出口)、`ROUTE_TABLE`(写死分流:`退款退货/False → knowledge` 保守覆盖,needs_knowledge=True 压倒意图名称)、`ChatGraphState(TypedDict, total=False)`、`new_turn_state(raw_query)` 返回 17 个临时键的显式初值字典(turn_messages 以 HumanMessage 起始,messages 键刻意缺席保留 checkpoint 历史)。
- `app/graph/errors.py`:`TurnAbortError(code)`——节点已发 error 帧后抛出中止图,驱动层捕获后不发 [DONE] 不提交本轮。
- `app/graph/events.py`:6 个事件构造函数(`ev_tool_start`/`ev_tool_end`/`ev_fixed_delta`/`ev_citations`/`ev_suggest_actions`/`ev_error`),统一 `"kind"` 键区分,供驱动层翻译成 SSE 帧。
- `tests/test_graph_state.py`:INTENTS 精确断言、14 组合参数化逐格核对、表尺寸/完备性、new_turn_state 全临时字段重置 + messages 缺席 + turn_messages 单条 HumanMessage,共 17 用例。
- 验证:`uv run pytest tests/test_graph_state.py tests/test_graph_smoke.py -v` → 19 passed 全绿(含 Task 1 的 2 个 smoke 用例)。

**我拒绝/纠偏了什么**:无用户纠偏。plan 与现状核对无矛盾(`app/graph/` 此前不存在、tests/test_graph_smoke.py 已就位、langchain_core/langgraph 依赖 Task 1 已装);实现完全按 plan 代码块逐字,无任何自定发挥。

**翻车与返工**:

- 无。TDD 红绿一次过:Step 2 红(ImportError: No module named 'app.graph',与计划预期完全一致)→ Step 4 一次 19 passed。

## 任务 8:前段节点——resolve_reference / classify_intent / 分流边(2026-09-18)

**我的关键原话**:

- 「执行实现计划……Task 8(前段节点——resolve_reference / classify_intent / 分流边)。先读该文件中 Task 8 一节(约第 1200-1400 行),严格按 Step 1-6 执行,代码块逐字使用。」
- 「parse_intent_output 严格:intent 限七类枚举、needs_knowledge 必须 JSON 布尔,字符串 "false"/缺字段/非法枚举一律 None;解析失败兜底 (售后, True) 走 knowledge,不得退回 business;模型异常发 upstream_error + TurnAbortError。」
- 「执行 Step 5:把 evals/probe_intent.py 的 parse 换成 from app.graph.nodes import parse_intent_output(消除预览版重复),重跑 uv run python evals/probe_intent.py 确认仍 8/8,结果记 dev-notes。」
- 「发现矛盾/走不通 → 停下报告等裁决,不得自换方案。」「不跑全量 pytest,不起 uvicorn。」
- 裁决(停报后):「A: 编译图驱动测试(计划预授权)」——测试改经 StateGraph 编译后 ainvoke 驱动,`app/graph/nodes.py` 一字不改。

**AI 的关键产出**:

- `app/graph/nodes.py`(plan 代码块逐字,一字未改):`GraphDeps` frozen dataclass(model/settings/retriever/store);`parse_intent_output` 正则提取首尾花括号 JSON,intent 必须 ∈ INTENTS 七类、`type(nk) is not bool` 拒掉字符串 "false"/缺失,任何不合法一律 None(8 参数化用例钉死:前后缀夹杂可解析、多余字段容忍、字符串 false/缺字段/非法枚举/非 JSON 全 None);`route_by_intent` 条件边只读 `state["route"]`;`build_front_nodes`——resolve_reference 原样透传 + node_trace,classify_intent 用 INTENT_PROMPT(`.replace` 占位)ainvoke,模型异常先 `writer(ev_error("upstream_error", ...))` 再抛 `TurnAbortError`,解析失败保守兜底 (售后, True) → knowledge 不退 business,成功查 ROUTE_TABLE 定 route;日志统一 logger `wayhelp.graph` 带 `node=`。
- `tests/test_graph_nodes.py`:12 用例(parse 8 参数化 + classify 三态 + resolve 透传)。classify 三态按裁决经编译图驱动(`_graph` helper:START → resolve_reference → classify_intent → END 的 StateGraph compile 后 `ainvoke`),`TurnAbortError` 经 `graph.ainvoke` 原样抛出、`pytest.raises` 断言不变;resolve_reference 不触 writer,保留裸调。
- `evals/probe_intent.py`:删本地预览版 parse 与 INTENTS 常量,改 `from app.graph.nodes import parse_intent_output`;重跑 8/8 命中(订单物流/库存/寄修流程/取消规则/多子问运费/纯投诉/投诉+运费/寒暄,与 Task 3 终跑判定表一致)。
- 验证:`uv run pytest tests/test_graph_nodes.py tests/test_graph_state.py -v` → **29 passed 全绿**。

**我拒绝/纠偏了什么**(计划预判风险成真,停报后按裁决走):

- plan Task 9 注记(1506 行,Self-Review 列为已知执行风险)假设「LangGraph 1.x 无 writer 上下文时 `get_stream_writer()` 返回 no-op writer,裸调节点测试安全」;实测已装 langgraph 1.2.11 裸调直接抛 `RuntimeError: Called get_config outside of a runnable context`——Step 4 首跑 3 failed/26 passed,三个 classify 测试全灭在 `classify_intent` 首行取 writer 处。按硬性要求停下报告(独立复现 + 真实测试双重证据),给出三案:A 编译图驱动测试(plan 1506 行自带的预授权兜底)/ B 节点内 safe writer helper / C 降级 langgraph 1.0.8;用户裁决 **A**:`nodes.py` 与 plan 逐字一致,仅测试文件的三个 classify 用例从裸调改为 `_graph(...)` 编译驱动。Task 9-12 节点同样直接调 writer,后续任务测试沿用同模式。
- 其余无纠偏;parse 兜底/枚举严格性与硬性要求逐条对上。

**翻车与返工**:

- Step 2 红符合预期(No module named 'app.graph.nodes');Step 4 首跑 3 failed(上述 get_stream_writer 矛盾)→ 裁决 A 后重写测试驱动方式 → 复跑 29 passed。
- probe 改造首跑 `NameError: name 'parse' is not defined`:只替换了头部 import/删了本地 parse,调用点漏改 → 补 `parse_intent_output(...)` 后 8/8。

## 任务 9:强制检索与置信度闸——retrieve / confidence_gate / gate_fallback(2026-09-18)

**我的关键原话**:

- 「执行实现计划……Task 9(retrieve / confidence_gate / gate_fallback 节点)。先读该文件中 Task 9 一节(约第 1404-1626 行),严格按 Step 1-5 执行,代码块逐字使用。」
- 「沿用 Task 8 裁决:get_stream_writer 在 langgraph 1.2.11 裸调上下文抛 RuntimeError,凡 writer 涉及的测试一律经编译图驱动断言(plan 第 1506 行注的预授权路径);nodes.py 实现代码不为迁就测试加防护。」
- 「三态语义:unavailable(NOTE_UNCONFIGURED/REBUILDING/REBUILD_REQUIRED/超时/异常,带 retrieval_error_code,即使 low_confidence=True 也不入池)、low_confidence(含 NOTE_NOT_BUILT 零命中)、ok;快照必须可 JSON 序列化。」
- 「RetrievalResult/KnowledgeHit/assemble_evidence/NOTE_* 的实际字段名以 app/knowledge/retriever.py 为准适配,语义不动。」「发现矛盾/走不通 → 停下报告等裁决,不得自换方案。」「不跑全量 pytest,不起 uvicorn。」

**AI 的关键产出**:

- 动手前交叉核对无矛盾:plan 用例/实现引用的字段与 `app/knowledge/retriever.py` 实际完全一致——`RetrievalResult` 9 字段、`KnowledgeHit` 8 字段均可按 plan 的关键字构造,`assemble_evidence(hits, *, max_items, budget_chars, overhead_chars)` keyword-only 签名吻合,`NOTE_UNCONFIGURED/NOT_BUILT/REBUILDING/REBUILD_REQUIRED` 四常量、`Evidence.to_dict` 就位;settings 三字段(`knowledge_tool_timeout_seconds`=20/`rerank_top_n`/`max_tool_result_chars`)、prompts 两常量、`ev_fixed_delta` 均在——零适配、未触发上报。
- `app/graph/nodes.py` 追加(plan 代码块逐字,顶部 import 补 `AIMessage`):`snapshot_retrieval`(RetrievalResult → 纯数据快照,`asdict` 展开 hits 与 query_plan,不含运行时对象,测试钉死 `json.dumps` 可序列化);`_STATE_NOTE_CODES`(三个配置/维护态 note → 错误码);`build_knowledge_nodes`——`retrieve`(retriever=None → unavailable/kb_unconfigured;`wait_for(to_thread(search), timeout=knowledge_tool_timeout_seconds)` 超时与异常同归 unavailable/kb_unavailable,本层不重试;`_STATE_NOTE_CODES` 命中优先于低置信判定——即使 low_confidence=True 也带 retrieval_error_code 入 unavailable 不入池;NOTE_NOT_BUILT 等其余 note 按 low_confidence → low_confidence/ok)、`confidence_gate`(low_confidence → low_conf_source=retrieval_low_conf + low_conf_reason 五字段;ok → KnowledgeHit 从快照重建 + `assemble_evidence(rerank_top_n, max_tool_result_chars, overhead 200)` → evidence dict 列表;unavailable 原样放行)、`route_after_gate`(status=="ok" → main_agent 否则 gate_fallback)、`gate_fallback`(unavailable → KB_UNAVAILABLE_ANSWER,其余 → REFUSAL_ANSWER;发 `ev_fixed_delta`;写 final_text/turn_messages 追加 AIMessage/node_trace)。
- `tests/test_graph_nodes.py` 追加 5 用例:ok 快照可序列化且 hits 逐字段还原、NOTE_UNCONFIGURED/NOTE_REBUILDING 即使 low=True 也 unavailable 带码、TimeoutError → unavailable/kb_unavailable、NOTE_NOT_BUILT 零命中 → low_confidence + 池字段 + route_after_gate=gate_fallback、gate_fallback 双话术 + REFUSAL_ANSWER 进 turn_messages。其中仅 `test_gate_fallback_texts` 触 writer,按 Task 8 裁决 A 经 `_gate_graph`(START → gate_fallback → END 编译图)驱动,断言与 plan 逐字一致;retrieve/confidence_gate/route_after_gate 不触 writer,保留裸调。`nodes.py` 未加任何防护。
- 验证:`uv run pytest tests/test_graph_nodes.py -v` → **17 passed 全绿**(Task 8 既有 12 + 新 5)。

**我拒绝/纠偏了什么**:无用户纠偏。plan↔现状核对零矛盾(字段名全数吻合,无需适配);唯一计划内偏差即 Task 8 已裁决的 writer 裸调问题,本任务直接沿用既定裁决路径(编译图驱动)处理 `test_gate_fallback_texts`,属方案内动作,未触发新上报;`nodes.py` 实现一字未改、未加防护。

**翻车与返工**:

- 无。TDD 红绿一次过:Step 2 红(`ImportError: cannot import name 'build_knowledge_nodes'`,与计划预期一致)→ Step 4 一次 17 passed。

## 任务 10:complaint_reply / chitchat_reply 固定回复节点(2026-09-18)

**我的关键原话**:

- 「执行实现计划……Task 10(complaint_reply / chitchat_reply 固定回复节点)。先读该文件中 Task 10 一节(约第 1630-1706 行),严格按 Step 1-4 执行,代码块逐字使用。」
- 「沿用 Task 8 裁决:两节点都调 get_stream_writer,测试一律经编译图驱动断言(plan 预授权路径);nodes.py 不加测试防护。」
- 「complaint 的 suggested_actions 结构逐字:[{"action": "transfer_human", "label": "转人工"}, {"action": "create_ticket", "label": "建工单", "ticket_type": "投诉"}];两节点只写 final_text/turn_messages/suggested_actions 并发一次性 fixed_delta,不在节点内发 suggest_actions 帧。」
- 「发现矛盾/走不通 → 停下报告等裁决,不得自换方案。」「不跑全量 pytest,不起 uvicorn。」

**AI 的关键产出**:

- 动手前交叉核对无矛盾:`COMPLAINT_REPLY`/`CHITCHAT_REPLY` 在 `app/prompts/service.py` 就位(逐字核对);state 的 `suggested_actions`/`node_trace`/`turn_messages` 键与 `new_turn_state` 初值(空列表)齐全;`build_fixed_nodes()` 不依赖 GraphDeps,零适配、未触发上报。
- `app/graph/nodes.py` 追加(plan 代码块逐字):`COMPLAINT_ACTIONS` 模块级常量(transfer_human 无 ticket_type / create_ticket 带 ticket_type=投诉——两按钮互相独立);`build_fixed_nodes()`——`complaint_reply` 发一次性 `ev_fixed_delta(COMPLAINT_REPLY)` + 写 final_text/suggested_actions(`[dict(a) for a in COMPLAINT_ACTIONS]` 拷贝防共享可变)/turn_messages 追加 AIMessage/node_trace;`chitchat_reply` 同构但零模型调用、不写 suggested_actions(state 保持初值 [])。两节点均**不发 suggest_actions 帧**(log 提交成功后 Task 12 驱动层才发)。
- `tests/test_graph_nodes.py` 追加 2 用例:complaint 双独立按钮逐字段断言(action 顺序、transfer_human 无 ticket_type 键、create_ticket ticket_type=投诉)+ 话术进 turn_messages;chitchat 零模型零建议按钮。两节点首行均取 writer,按 Task 8 裁决 A 经 `_fixed_graph`(START → 节点 → END 编译图)驱动,断言与 plan 逐字一致;`nodes.py` 未加任何防护。
- 验证:`uv run pytest tests/test_graph_nodes.py -v` → **19 passed 全绿**(Task 9 既有 17 + 新 2)。

**我拒绝/纠偏了什么**:无用户纠偏。plan↔现状核对零矛盾;writer 裸调偏差沿用 Task 8 既定裁决路径(编译图驱动),属方案内动作,未触发新上报;`nodes.py` 实现一字未改、未加防护。

**翻车与返工**:

- 无。TDD 红绿一次过:Step 2 红(`ImportError: cannot import name 'build_fixed_nodes'`,与计划预期一致)→ Step 4 一次 19 passed。

## 任务 11:main_agent 手写 ReAct 节点(2026-09-18)

**我的关键原话**:

- 「执行实现计划……Task 11(main_agent 手写 ReAct 节点,核心任务)。先读该文件中 Task 11 一节(约第 1710-2204 行),严格按 Step 1-9 顺序执行,代码块逐字使用;这是最大任务,Steps 多,按顺序做不要跳。」
- 「注册表只有 query_order/query_product/query_logistics/suggest_options,create_ticket/query_faq 绝不进绑定列表与注册表。」
- 「沿用 Task 8 裁决:get_stream_writer 在 1.2.11 裸调抛 RuntimeError,触 writer 的测试经编译图驱动;agent_node.py 不加测试防护。」
- 「发现矛盾/走不通 → 停下报告等裁决,不得自换方案。」「不跑全量 pytest,不起 uvicorn。」

**AI 的关键产出**:

- 动手前交叉核对全绿:`merge_tool_call_chunks`/`finalize_tool_calls`/`fit_tool_context(…, protected_from=)`/`ToolRegistry`/`ToolExecutor(…, write_tools=)`/`MOCK_TOOLS`(三只读)/events/errors/state/`AGENT_BUDGET_ANSWER`/`FALLBACK_ANSWER` 签名与 plan 用法一致,零适配。实测钉死 langgraph 1.2.11 两行为:裸调 `get_stream_writer()` 抛 RuntimeError;编译图内省缺的 `messages` 键呈现为 `[]`(故 plan 的 `state["messages"]` 仅图内成立)。
- `app/config.py` +`max_agent_steps`/`max_agent_tokens`/`checkpoint_db_path`,Step 1 `tests/test_config.py` 17 passed。
- `app/chains/tool_chat_chain.py` +`build_agent_context`(逐字):布局 [system, …历史…, evidence?, …本轮…],裁剪委托 `fit_tool_context` 区间语义(evidence 起点即保护区起点),无历史直接判定,放不下返回 None。
- `app/graph/agent_node.py` 新建(plan 代码块逐字 + 头部补 `get_stream_writer` import):行为契约 7 条落地——①无 tool_calls 收敛、空文本 FALLBACK_ANSWER 经 fixed_delta;②调用前校验(`_calls_legal` ≤5/唯一/id≤64)+逐个执行,伪工具走 `_handle_suggest_options` 零副作用不发徽章;③注册表 = 三只读 + suggest_options,伪造 create_ticket/query_faq → executor unknown_tool,`write_tools=set()` 零写权限;④步数检查在模型调用前,兜底前 ToolMessage 补齐不留悬空 tool_calls;⑤调用前预留 `est_input+max_output_tokens`、不足即兜底不再调模型,调用后有效 usage 替换预留/缺失保留预留,accounting ∈ usage/estimated/mixed;⑥`length`/超字数/模型异常/上下文放不下 → ev_error + TurnAbortError 四路 abort;⑦每步经 build_agent_context 重建裁剪。`app/graph/nodes.py` GraphDeps +`system_prompt: str = ""`(Task 13 装配注入)。
- 测试:`tests/test_agent_node.py` 新建(循环 7 + 预算边界 3 = 10 用例),按 Task 8 裁决经编译图驱动(`_agent` 返回 START→main_agent→END 的 compile() 图,断言逐字);`tests/test_tool_chat_chain.py` 追加 rebuild 多组往返与 build_agent_context 保护区两钉(追加块 import 与文件头重名者合并不重复导入);`tests/conftest.py` FakeChunk/FakeStreamModel 支持 `("usage", {...})`。
- 验证:Step 6 → 7 passed;最终 `uv run pytest tests/test_agent_node.py tests/test_tool_chat_chain.py -v` → **26 passed 全绿**。

**我拒绝/纠偏了什么**:无用户纠偏。两处 plan↔现实偏差均走预授权路径,未触发上报:①plan 测试裸调节点(1.2.11 必炸)→ 按 Task 8 裁决改经编译图驱动,断言不动、`agent_node.py` 未加防护;②plan Step 9 git add 清单漏了 Step 2 改动的 `tests/test_tool_chat_chain.py` → 按本次指令给定文件清单提交。

**翻车与返工**:

- 无。TDD 红绿一次过:Step 3 红(`ModuleNotFoundError: No module named 'app.graph.agent_node'`,与计划预期一致)→ Step 6 一次全绿。
- 记录在案的小勘误:plan Step 6 写「Expected: 6 PASS」但 Step 2 实定义 7 个用例,系 plan 清点笔误,按实际 7 执行;`_tool_schema_tokens` 用 pydantic `.schema()` 触发 DeprecationWarning(plan 代码块逐字,已 try/except 包裹,无害)。

## 任务 12:log 节点——提交、入池、提交后发帧(2026-09-18)

**我的关键原话**:

- 「执行实现计划……Task 12(log 节点——提交、入池、提交后发帧)。先读该文件中 Task 12 一节(约第 2208-2414 行),严格按 Step 1-4 执行,代码块逐字使用。」
- 「沿用 Task 8 裁决:get_stream_writer 在 1.2.11 裸调抛 RuntimeError,触 writer 的测试经编译图驱动(plan 测试代码块中 log 节点带 config 参数的用法以编译图驱动方式适配,断言语义不变);nodes.py 不加测试防护。」
- 「行为契约:入池两来源(retrieval_low_conf 用 low_conf_reason;retrieval_status==ok 且 final_text.strip()==REFUSAL_ANSWER → self_check,reason 含 evidence ref_no/chunk_id),每轮至多一条,故障/预算/普通答复不入池;citations 五条件齐备才发;commit shield,提交成功后才发 citations/suggest_actions 帧;返回 messages=turn_messages 与 source_message_id。」
- 「发现矛盾/走不通 → 停下报告等裁决,不得自换方案。」「不跑全量 pytest,不起 uvicorn。」

**AI 的关键产出**:

- 动手前交叉核对无矛盾:`StoredMessage(role, content, tool_calls, tool_call_id)`/`LowConfidenceRecord(raw_question, source, reason, conversation_id)`/`commit_turn(sid, messages, low_confidence=)`→`CommitTurnResult.source_message_id`/`store.low_confidence` 列表/`wrap(content, ok, error_code, max_chars)`/`ev_citations(list)`/`ev_suggest_actions(id, options)`/`AGENT_BUDGET_ANSWER`/`InMemorySessionStore(10, 100, 8000)` 与 plan 用法逐字段吻合,零适配、未触发上报。
- `app/graph/nodes.py` 追加(plan 代码块逐字,import 按文件惯例——`asyncio`/`HumanMessage`/`json`/`re`/`AIMessage`/`REFUSAL_ANSWER` 已具备则复用,新增 `contextlib`/`ToolMessage`/`ev_citations`+`ev_suggest_actions`/`AGENT_BUDGET_ANSWER`/`LowConfidenceRecord`+`StoredMessage`/`wrap`):`_to_stored`(Human→user 行、AI→assistant 行 content None 容忍纯工具调用、Tool→wrap envelope + status!=error 定 ok + additional_kwargs.error_code 兜底 tool_error,SystemMessage 不落库)、`_build_low_conf`(两源择一至多一条:retrieval_low_conf → reason 为 low_conf_reason 的 JSON;ok+REFUSAL → self_check,reason 为 evidence ref_no/chunk_id 的 JSON;其余 None)、`_should_cite`(五条件:route==knowledge、status==ok、evidence 非空、final 含 `[n]` 角标、不等于 REFUSAL/BUDGET)、`build_log_node`(commit 经 `ensure_future`+`shield`,CancelledError 等事务落地再 re-raise;提交成功后才发 citations/suggest_actions 帧;logger 带 steps/tokens/accounting;返回 messages=turn_messages 此刻才追加跨轮历史 + source_message_id + node_trace)。
- `tests/test_graph_nodes.py` 追加 5 用例:commit 成功返回 source_message_id 且 messages==turn_messages 且落库 [user, assistant];low_confidence 入池(source/raw_question);ok+REFUSAL 入 self_check 池(reason 含 chunk_id);unavailable/BUDGET 不入池;`_should_cite` 四分支纯函数。log 节点首行取 writer,按 Task 8 裁决 A 经 `_log_graph`(START → log → END 编译图,`ainvoke(st, config=_config(sid))` 传入 thread_id)驱动,断言语义与 plan 一致;`nodes.py` 未加任何防护。
- 验证:`uv run pytest tests/test_graph_nodes.py -v` → **24 passed 全绿**(既有 19 + 新 5)。

**我拒绝/纠偏了什么**:无用户纠偏。plan↔现状核对零矛盾(签名全数吻合,无需适配);plan 测试裸调 `node(st, config)` 会踩 Task 8 已裁决的 writer RuntimeError,按本次指令沿既定路径(编译图驱动,config 经 ainvoke 传入)适配,断言不动,属方案内动作,未触发新上报;`nodes.py` 实现一字未改、未加防护。

**翻车与返工**:

- 无。TDD 红绿一次过:Step 2 红(`ImportError: cannot import name 'build_log_node'`,与计划预期一致)→ Step 4 一次 24 passed。

## 任务 13:图装配 + ChatService 驱动重构 + main.py 装配 + 旧测试改造(2026-09-18)

**我的关键原话**:

- 「执行实现计划……Task 13(图装配 + ChatService 驱动重构 + main.py 装配 + 旧测试改造)。这是全章最大任务,先完整读 Task 13 一节……严格按 Step 1-10 顺序执行,代码块逐字使用。」
- 「ScriptedChatModel 必须真 BaseChatModel 子类;若 BaseChatModel.astream 实测不发回调,先查 Context7 langchain-core 文档,仍不通立即停下报告——不得 monkeypatch 伪造回调。」
- 「chat_service.py:删 toolset_factory,加 set_graph;FALLBACK_ANSWER 改从 app.prompts.service import 并删本文件旧定义;prepare 三段语义与 release_turn/aclose 契约保留;messages 流按 langgraph_node=="main_agent" 过滤。」「test_orchestration.py 删除;旧『两次调用』专属契约退役,HTTP/SSE 帧协议契约尽量保留。」「发现矛盾/走不通 → 停下报告等裁决,不得自换方案。」
- 五项裁决(两次停报后):①批准测试脚手架修复(stub store + `isinstance(c, AIMessageChunk)` 过滤,断言与 app 代码不动);②批准 chat_service messages 分支加一行 AIMessageChunk 护栏(偏离逐字的唯一一处);③批准 conftest 给 FakeStreamModel 加 `ainvoke`(罐头业务意图,不消耗脚本不记 received);④允许删 test_chat_api.py::test_sse_citations_frame_schema_and_order(语义迁 test_chat_api_tools);⑤再删 test_sse_happy_path_frames/test_sse_json_escaping 并把帧序/JSON 转义契约迁入 test_chat_api_tools(迁移+删)。

**AI 的关键产出**:

- `app/graph/builder.py`(plan 逐字):`build_chat_graph(deps, checkpointer)`——九节点装配(resolve_reference → classify_intent → 四出口条件边;retrieve → confidence_gate → 二态条件边;四出口全汇 log → END),`route_after_gate` 作条件边函数未误加为节点。
- conftest 追加 `ScriptedChatModel(BaseChatModel)`(plan 逐字):真 Runnable,_generate/_astream 双路径脚本制;实测回调链通畅——但**langchain-core 1.6.2 的 `ainvoke` 在子类覆写 `_astream` 时走流式路径聚合**(非 _generate),脚本制下两路径恰好等价故无碍,分类输出照常弹首段脚本。
- `app/services/chat_service.py` 重构:删 toolset_factory/_tool_calls_legal/两次调用编排,加 set_graph 与 `SuggestActionsEvent`,`PreparedTurn` 删 messages 字段,prepare 三段(长度闸 → check_input_budget 早闸 → 会话解析 → 锁),stream() 驱动 `graph.astream(stream_mode=["messages","custom"])`、messages 按 `langgraph_node=="main_agent"` + **AIMessageChunk 护栏(裁决②)**过滤、custom 经 `_translate_custom` 翻译六类事件、TurnAbortError 静默收尾不发 DONE;FALLBACK_ANSWER 改自 prompts import。
- `app/routers/chat.py` +suggest_actions 帧序列化;`app/main.py`:ChatOpenAI 加 stream_usage=True,GraphDeps 装配,注入路径即刻 InMemorySaver、owns_runtime 路径 lifespan 内 AsyncSqliteSaver 托管启停。
- 测试改造:test_orchestration.py 删除(无工具单次收敛/工具序列/空文本兜底三个语义新家已在 test_agent_node);test_chat_service.py 按图重写 22 用例(prepare 三段、锁四条、上游错误两段+脱敏、bind_tools 失败 internal_error、output_too_long/finish length/预算兜底、envelope 真实 error_code、DONE 断连、取消不提交半轮、同/异 session 并发、chitchat/complaint、硬闸门/自评/citations/故障不入池);test_chat_api_tools.py 重写 11 用例(query_faq 全删,硬闸门/citations 迁预检索路径,迁入帧序与 JSON 转义契约,新增 suggest_actions 帧形状与顺序);test_spec11_safety.py 重接 2 用例(commit shield 窗口经图、tool_context_too_long 前置预算经长证据触发),建单副作用场景退役;test_chat_api.py 删 3 用例(裁决④⑤)。test_agent_node 空文本兜底用例 Task 11 已有,无需补。
- 验证:Step 5 图冒烟 2 PASS;最终 `uv run pytest` 全量 **425 passed**。

**我拒绝/纠偏了什么**(两轮停报、五项裁决,全部 AI 上报后经批准执行):

1. **plan 测试笔误(plan 代码在其环境下无解)**:冒烟测试 `_deps` 传 store=None,但 builder 四出口全汇 log(首行即 commit_turn)必炸;真 InMemory store 又需 prepare 预创建 session。AI 最小实验(stub store / 真 store)确证唯一缺陷后停报,裁决①照 Task 1 先例只修测试脚手架。
2. **langgraph 1.2.11 messages 模式额外发射**:逐帧 dump 证实除 token 外还发节点级 HumanMessage 回显与聚合终帧 AIMessage(同带 langgraph_node 元数据;plan 按 1.0.8 文档写,Context7 现行文档亦未载)——plan 测试 2 过滤器与 **Step 6 驱动层逐字代码都会把用户问题回显+答复全文重复发给用户**。裁决②批准在逐字代码块加一行 `isinstance(chunk, AIMessageChunk)` 护栏(唯一偏离点,带注释)。
3. **test_chat_api.py(计划 Files 清单遗漏)**:FakeStreamModel 无 ainvoke 且非 Runnable 不发电回调——裁决③④⑤分三步处置:ainvoke 罐头适配救活 upstream_error/second_turn/aclose 等用例;无 retriever 注入点救不活的 citations 旧测与 delta 内容断言类两条(happy_path/json_escaping)按「迁移+删」处置,契约全部迁入 test_chat_api_tools 重写版。

**翻车与返工**:

- Step 5 首跑 2 failed(store=None,矛盾 1);实验修复后 2 PASS。
- 全量首跑(带 -x)在 test_spec11 旧用例挂死:graph=None 时 stream 立即返回、monkeypatch 的 commit 永不执行、`entered.wait()` 永等——属待重写文件的预期中间态,定位后按处置表重写消解。
- 重写后 3 failed 返工一轮:①GatedScriptedModel 暴露 ainvoke 走 `_astream` 聚合的 1.6.2 行为(分类拿到「开始结束」→ 兜底 knowledge → KB_UNAVAILABLE),按「首条消息 Human=分类/System=agent」区分调用方修复;②spec11 `_service` 手误硬编码 retriever=None,长证据检索器没注入,补参数修复。复跑全量 425 passed。

## 任务 14:前端——suggest_actions 按钮组与转人工模拟(2026-09-18)

**我的关键原话**:

- 「执行实现计划……Task 14(前端——suggest_actions 按钮组与转人工模拟,Vibe 例外无 TDD)。先读该文件中 Task 14 一节(约第 2754-2874 行),严格按 Step 1-5 执行,CSS/JS 代码块逐字使用。」
- 「chat.html 是 Vibe 例外:字符串断言 + 人工点验,不改既有逻辑结构;JS 追加在 script IIFE 内并接入 parseEvents 分发链(与 citations 分支并列);按钮各自独立锁定,转人工纯前端不发请求,建工单 fetch 捕获时绑定 user_id/session_id/source_message_id/ticket_type。」
- 「注意 plan 字符串里的中文标点(「您好，我是客服小猫，请问有什么可以帮您的？」是全角逗号句号),与 test_chat_page.py 断言逐字一致。」「不要起 uvicorn(人工点验由 master 做);不跑全量 pytest,但 test_chat_page.py 必须全绿。」「按 Task 14 Step 5 的 commit 命令提交,只提交 app/static/chat.html tests/test_chat_page.py dev-notes/ch05.md。」

**AI 的关键产出**:

- 动手前交叉核对零矛盾:9 个 needle 中「转人工」「建工单」已由既有 SUGGESTIONS「帮我转人工处理」与 TOOL_LABELS「创建工单」覆盖,其余 7 个全部落在 plan 的 JS 代码块内;全角逗号问候语 plan↔断言逐字一致(测试通过即字节级证实)。
- `tests/test_chat_page.py` 追加 `test_chat_page_action_buttons_logic`(9 needle 字符串契约);plan 代码块未带 Path import(照抄会 NameError),本文件无共享 helper,按既有函数内 `from pathlib import Path` 惯例补一行(plan 注明「有 helper 则复用」的从惯例延伸)。
- `app/static/chat.html`:CSS 逐字追加于 `<style>` 内 suggestion chips 段之后(仅新增段注释);JS(KITTEN_GREETING/addSystemHint/renderActions——捕获时绑定、按钮各自独立锁定、转人工纯前端零请求、建工单 fetch 绑 user_id/session_id/source_message_id/ticket_type、失败不自动重试)逐字追加于 script IIFE 内 addFeedback 之后、setStreaming 之前;parseEvents 分发链 citations 分支后并列插入 suggest_actions 分支。既有逻辑零改动。
- 验证:`uv run pytest tests/test_chat_page.py -v` → **7 passed 全绿**(既有 6 + 新 1)。Step 1 红(缺 suggest_actions)→ CSS/JS 落位后一次绿。
- 人工点验(起服务点按钮)按指令留给 master,本任务未起 uvicorn、未跑全量 pytest。

**我拒绝/纠偏了什么**:无用户纠偏,plan↔现状核对零矛盾,未触发上报;唯一微偏离即上述测试函数内补 Path import 一行,属惯例延伸而非方案变更。

**翻车与返工**:

- 无。Step 1 红(缺 suggest_actions,预期)→ Step 4 一次 7 passed。
- 待办移交:plan Step 4 的人工点验(说「我要投诉」→ 双独立按钮;点「转人工」→ 提示+小猫问候且另一按钮仍可用;点「建工单」→ 出工单号;都不点继续发消息正常对话)未做,待 master 起服务(docker 在线)点验。

## 任务 15:验收集成、持久化测试、联调验证与文档(2026-09-18,收官)

**我的关键原话**:

- 「执行实现计划……Task 15(验收集成、持久化测试、联调验证与文档,收官任务)。先完整读 Task 15 一节(约第 2878-3326 行),严格按 Step 1-5 执行,测试代码块逐字使用。」
- 「验收:uv run pytest tests/test_ch05_acceptance.py tests/test_graph_persistence.py -v 全绿 → 全量 uv run pytest 绿(Docker 在线)。验收 11 条语义不得改;红则按 spec 修实现,不得改验收语义。」
- master 预裁决(当时定为唯一一处):「生产侧 wayhelp.graph 的 INFO 日志无 handler 不可见,而 Task 15 Step 3 联调要求『看 retrieve/confidence_gate 日志』。在 app/main.py create_app 内加最小日志配置(logging.basicConfig() 提供 root handler + logging.getLogger("wayhelp.graph").setLevel(logging.INFO) 或等价最小写法),不动其他逻辑。」
- 「Step 3.2 DeepSeek stream_usage 探针必须真跑并记录输出;若供应商拒绝 stream_options 或 usage 恒 None → 立即停下报告,不得自行改方案。」
- 「Step 3.3 真人五连 master 已代跑通过(结果:投诉→suggest_actions 双按钮帧+DONE;动作端点建单 T20260918053921f347382d74fe 返回待处理;退货政策→94 delta+citations;物流→query_order/query_logistics 双 tool 徽章;你好→CHITCHAT_REPLY 逐字),把这五结果记入 dev-notes 联调段即可,不需重复跑。」
- 执行中三案停报后一次批齐:「批准补日志(推荐)」「批准换 DbSessionStore(推荐)」「批准改测试脚手架(推荐)」。

**AI 的关键产出**:

- `tests/test_ch05_acceptance.py`(plan 代码块逐字,11 用例)与 `tests/test_graph_persistence.py`(逐字 + 裁决 C 两行脚手架,2 用例);验收命令 13 passed 全绿。
- 三处 plan 缺口修复(全部先停报取证、经裁决后动工):**A** `app/graph/nodes.py` confidence_gate 入口补 `logger.info("node=confidence_gate status=%s", status)`——Task 15 验收 1 断言过闸路径可见 `node=confidence_gate`,而 Task 9 自己的代码块只在 low_confidence 分支打日志,系 plan 内部矛盾(实现此前逐字照 Task 9,非跑偏);**B** `app/main.py` 注入路径(runtime.session_factory 非 None)store 换 `DbSessionStore(...)` 与生产同构——spec §13 验收 3/10 要求全链路消息/入池/动作端点写同一 DB 账本,而 Task 13 装配原样用 runtime.store,注入 db_sf 后 store 仍内存版(UUID sid、不落库),a3 404/a6 池空/a10、a11 `int(uuid)` 炸;已核对唯一受影响的既有测试 test_chat_action 不走流,不受换源影响,生产路径不动;**C** 持久化重开用例直接 `graph.ainvoke` 不经 prepare,真 InMemorySessionStore 未预创建会话在 log 节点 `KeyError: 's1'`——与 Task 13 裁决①同因,照先例修测试脚手架(`store.create` 后以返回 sid 作 thread_id,断言不动)。
- `app/main.py` 另加 master 预裁决的最小日志配置(`logging.basicConfig()` + `wayhelp.graph` setLevel INFO;pytest 期间 root 已有捕获 handler,basicConfig 幂等无副作用)。
- 验证:验收+持久化 **13 passed**;全量 `uv run pytest` **439 passed**(Docker 在线,含新 13 用例)。
- DeepSeek stream_usage 探针真跑(spec §7.3):`usage_metadata: {'input_tokens': 32, 'output_tokens': 80, 'total_tokens': 112, 'input_token_details': {'cache_read': 0}, 'output_token_details': {'reasoning': 78}}`——端点接受 stream_options,流式 usage 非 None,方案成立;进程退出期有一处 httpcore2 异步生成器关闭噪音(RuntimeError: generator didn't stop after athrow()),发生在结果打印之后,属 teardown 噪音不影响结论。
- 联调(master 代跑真人五连,服务 8000 跑当时代码,对应验收 1-5;任务 14 的人工点验待办由此收口):①「我要投诉」→ suggest_actions 双按钮帧(转人工/建工单互相独立)+ DONE;② 动作端点建单 **T20260918053921f347382d74fe** 返回「待处理」;③「退货政策是什么」→ 94 个 delta 帧 + citations 帧;④ 物流问题 → query_order/query_logistics 双 tool 徽章;⑤「你好」→ CHITCHAT_REPLY 逐字。
- 文档:README 架构段新增 ch05 节(LangGraph 图编排、图节点链、/v1/chat/action、checkpoints.db、验收命令)并同步测试数 439;AGENTS.md 目录约定加 `app/graph/` 与 `examples/`、当前状态更新为 ch05 完成(439 passed、stream_usage 已核实)、红线区补「聊天 Agent 无写权限,建工单唯一通道 POST /v1/chat/action」。

**我拒绝/纠偏了什么**:无用户纠偏;六红停报后三案均采纳推荐项一次批齐(补日志/换 DbSessionStore/改测试脚手架),批准范围即执行范围,无越权改动。

**翻车与返工**:

- 验收首跑 **6 红 7 绿**,三个互相独立的根因(证据齐全后停报):①a1 过闸无 confidence_gate 日志(plan 内部矛盾);②a3/a6/a10/a11 store 装配缺口;③持久化重开用例 KeyError。裁决 A/B/C 修复后复跑 13 绿,返工一轮、未扩散。
- 全量 pytest 修复后一次 439 passed;探针一次通过(usage 非 None);文档与提交无翻车。

## 阶段 4:code review 结论(2026-09-18)

**我的关键原话**:「dev-notes/ch05.md 末尾追加……『## 阶段 4:code review 结论』……写本次 master 逐任务独立验证与 6 次裁决的记录(任务 1 冒烟断言笔误、任务 2 sys.path 先例、任务 8 writer 行为、任务 13 五项、任务 15 日志配置),注明正式的 superpowers code-review 技能跑批留待用户决定。」

**AI 的关键产出**(本段为记录汇总):

- master 逐任务独立验证:15 个任务每个都要求真跑验收命令并核对输出(各任务段「验证」行),收官口径 = 验收/持久化 13 passed + 全量 439 passed + 联调真人五连通过 + 探针 usage 非 None。
- 六轮裁决在案:
  1. **任务 1 冒烟断言笔误**:node 无条件 `return {"tag": "seen"}` 令二次输入断言在任何版本下无解 → 裁决改 `state["tag"] or "seen"`,两条断言原样保留(plan 笔误修复,非选型变更)。
  2. **任务 2 sys.path 先例**:examples/bare_agent.py 脚本运行 `ModuleNotFoundError: app` → 照 evals/ 既定先例加两行引导,plan 其余一字不动;同裁决预先覆盖 Task 3 probe_intent.py。
  3. **任务 8 writer 行为**:langgraph 1.2.11 裸调 `get_stream_writer()` 抛 RuntimeError(plan 按 1.0.8 行为假设)→ 裁决 A:测试经编译图驱动,实现代码不加防护;沿用至任务 9-12 全部 writer 类测试。
  4. **任务 13 第一轮**:冒烟测试 `_deps` 传 store=None 但四出口全汇 log 必炸、真 store 又需 prepare 预创建 → 裁决①修测试脚手架(stub store)。
  5. **任务 13 第二轮(四项)**:langgraph 1.2.11 messages 模式额外发射节点级回显/聚合终帧会重复输出 → 裁决②驱动层加一行 `isinstance(c, AIMessageChunk)` 护栏(全章唯一偏离逐字代码处);FakeStreamModel 非 Runnable 不发电回调 → 裁决③ conftest 补罐头 ainvoke;裁决④⑤旧 citations/happy_path 用例按「迁移+删」处置,契约全部迁入 test_chat_api_tools 重写版。
  6. **任务 15 日志配置**:生产侧 wayhelp.graph 的 INFO 无 root handler 不可见 → create_app 内 `logging.basicConfig()` + setLevel(INFO) 最小配置,不动其他逻辑。
  另:任务 15 执行中又停报一次、新增三项裁决(A 补 confidence_gate 日志 / B 注入路径换 DbSessionStore / C 持久化测试脚手架,详见任务 15 段)——plan 缺口裁决合计 **9 项**。
- 正式的 superpowers code-review 技能跑批(Standards/Spec 双轴)本轮**未执行**,留待用户决定是否补跑;本段结论基于 master 全程逐任务人工评审。

**我拒绝/纠偏了什么**:无。

**翻车与返工**:无(本阶段为记录汇总,无代码动作)。

## 阶段 5:finish(2026-09-18)

**我的关键原话**:「按 Task 15 Step 5 的 commit 命令提交,只提交上述文件。完成后一句话总结:验收/持久化/全量 pytest 结果 + stream_usage 探针结论 + commit hash。」

**AI 的关键产出**:

- 验证收口:evidence 在案——`tests/test_ch05_acceptance.py tests/test_graph_persistence.py` 13 passed;全量 439 passed;探针 usage 非 None;真人五连 master 代跑通过。
- 提交:按 plan Step 5 的 commit 命令,文件集 = 两份新测试 + README.md + AGENTS.md + dev-notes/ch05.md + 裁决 A/B 的修复文件(app/graph/nodes.py、app/main.py),不裹挟其他改动;工作区干净收尾。
- ch05 全章 15 任务收官:主链路 LangGraph 图驱动、聊天 Agent 零写权限、建单唯一通道动作端点、SQLite checkpoint 持久化,spec §13 验收 11 条全部有自动化钉。

**我拒绝/纠偏了什么**:无。

**翻车与返工**:无(收尾一次过;任务 15 的六红返工已记该段)。

