# Ch03 开发过程记录 —— RAG 基础:知识库与向量语义检索

## 阶段 1:Brainstorm 定稿(2026-09-11)

**用户关键原话**
- 「我要用 Superpowers 模式给客服系统建知识库,把 query_faq 的内部实现从关键词查表升级成向量语义检索,工具的入参出参契约保持不变。」
- 「技术栈:嵌入模型 BGE-M3;向量库 Milvus,MySQL 当原文权威源」「本章不做:关键词召回、混合检索、重排,本章只跑 dense 向量单路」
- 「涉及具体库、框架、API 的用法,一律先用 Context7 MCP 查最新官方文档和接口定义再动手,别凭记忆写」
- 「上面点名的技术选型是定死的,实现中发现矛盾或走不通,停下来问我,不要自行换方案」
- 「[ch03-ddl.sql] 是本次需要建表的sql」——knowledge_chunks + qa_extraction_staging 两张表 DDL 由用户手写提供(沿用 ch02 惯例)
- 「产出不是可单测代码的任务(纯 Prompt、数据类),把 TDD 那步换成拿标注样例或评估集跑一遍验证」
- 「过程留痕:每完成一个阶段就补一段,记四样……不许收尾时一次性补记」

**澄清问答(AskUserQuestion 五轮)**
- 嵌入接入:用户答「用硅基流动的,token后面我填进去」——**远程 Embedding API**(OpenAIEmbeddings 指硅基流动,model=BAAI/bge-m3),否掉我推荐的本地 sentence-transformers(2.3GB 模型下载)
- Milvus 部署:用户答「用 Milvus Lite 嵌入式」——否掉我推荐的 Docker Compose standalone(与 ch02「测试真实基础设施同构」惯例有出入,但用户拍板本地文件;测试改用 Milvus Lite tmp 文件,仍是真实向量库)
- 数据来源:用户选「都由我造演示数据」——演示 Markdown 文档我写入仓库;历史对话直接挖现有 MySQL conversations/messages 表
- 任务形态:用户答「弄个命令可以启动job就可以,我可以直接选择部署或直接执行」——CLI 命令(ingest_docs / mine_qa),不内嵌 APScheduler
- 方案选择:用户选 **B(裸 pymilvus + OpenAIEmbeddings)**,否掉 A(langchain-milvus 全家桶)、C(自抽 VectorStore 协议)

**我的关键产出**
- 路径分类:architectural(新增知识库子系统:离线建库 + 挖矿 + 在线检索替换)
- 三段设计,逐段用户确认(§1 模块结构+§2 文档切分 → §3 双写+§4 挖知识 → §5 检索+§6 错误处理+§7 测试验收,三次「ok」)
- spec:`docs/superpowers/specs/2026-09-11-knowledge-base-ch03-design.md`(12 节)
- 关键设计点:Milvus 集合只存 id+vector(原文回 MySQL 取,权威源单一);Phase 1 load / Phase 2 vectorize 两阶段,中断重跑天然成立;Milvus 写入用 upsert 兜底「Milvus 已写、MySQL 未回填」的中断缝隙;挖矿 source_ref 判重实现「同一会话只挖一次」;去重为规范化文本确定性匹配,不引第三个模型
- Context7 核对(动手前):pymilvus MilvusClient/create_collection/upsert/search 签名;OpenAIEmbeddings 构造与 embed_query/embed_documents;**抓到真坑**:第三方模型必须 `check_embedding_ctx_length=False` 跳过 tiktoken,已写入 spec §2/§12

**用户拒绝/纠偏**
- 嵌入接入否本地库、Milvus 否 Docker standalone(均见上);方案 A/C 被否
- 三段设计确认均无修改,一次通过

**翻车与返工**
- 暂无(设计阶段未动代码)。

## 阶段 2:Spec 复审修订(2026-09-11)

**用户关键原话**
- 「spec文档已经让gpt6进行review并修改了」——spec 经 GPT-6 复审修订(302 行,替我原稿 156 行),落实八项评审意见:QA 来源、独立抽取进度、文档位置幂等、Milvus Lite 依赖、缺 Key 降级、异常重试、切分边界、检索评估。

**复审主要修订(GPT-6 稿并入,逐项核真)**
- `knowledge_chunks` 新增 `source_doc` + `chunk_index` + 唯一索引 `uk_doc_chunk`:文档按「来源+位置」复用 ID,切分结果变化明确报错不覆盖;我原稿的「三格内容判重」全局幂等键被废(不同文档相同内容应分别落行),挖掘 QA 的内容去重改由 §7 独立处理
- 新增第三张表 `qa_mining_progress`(独立抽取进度):staging 不再兼任会话游标;成功但零 QA 也落进度;staging 行与进度行同事务提交;清空 staging 后仍可凭进度跳过
- 切分规格大幅收紧:`max_chunk_chars` 定义为最终 answer 上限(含重叠/表头/换行);重叠仅限同 section 内完整句后缀、不跨章节不与表格;无标点长句允许按字符硬切(不得无限递归/空块);表头+单行超限报错给行号,不得截断单元格
- 缺 Key 降级:`embedding_api_key` 允许为空,在线返回「知识检索未配置」note、应用正常启动且不构造 embedding 客户端;CLI/评估写库前显式报错
- 异常分级:OpenAI SDK 连接/超时/限流 + HTTP 408/409/5xx 映射 `RetryableKnowledgeError` 加入 executor 的 `RETRYABLE`,在线 SDK `max_retries=0` 防叠乘;耗尽走 `tool_unavailable` envelope,鉴权/参数/维度错误零重试走 `tool_error`
- 检索内部命中记录保留 `chunk_id`/`score`/`source_doc`/`chunk_index` 供评估比对,工具出参不增加调试字段;在线 Milvus 客户端延迟初始化、不建集合
- 评估升级为硬指标:≥20 正例 + ≥20 负例,固定 calibration/test 分片;阈值只用 calibration 搜索冻结;test 达标线 Recall@5 ≥ 90% 且负例误召回率 ≤ 10%;「邮费是多少」固定进 test 分片、不命中即失败;结果落 `evals/results/`,冻结阈值回写 Settings 发布默认值
- 依赖显式 `pymilvus[milvus-lite]` extra(GPT-6 查了 PyMilvus 3.0.1 包元数据,Lite 不随裸包);运维约束写明「停服跑 CLI、单 worker、独占本地库」
- 文档生命周期限定「首次导入 + 原样重跑」;恢复承诺限定同一份源数据+相同切分配置+持久化数据仍在的中断场景
- §4.5:现有 `sql/ch03-ddl.sql` 成为待同步基线,实现前必须完成 DDL 增量(两个来源字段+唯一索引+进度表)并同步 `db/init/03-ddl.sql` 逐字节一致 + 三个 ORM 模型;README 区分首次初始化与已有 ch02 数据卷的升级路径,不得删卷重建

**我的关键产出(自审)**
- 引用机制核真:executor.py:13 `RETRYABLE` 集合、:86/:121 `tool_unavailable`、:92/:122 `tool_error` 全部在位,修订稿的挂载点真实存在
- 一致性核对通过:`uk_doc_chunk` 下挖掘 QA 双 NULL 可多行(MySQL 唯一索引 NULL 语义)与 §4.1 自洽;§7 步骤 4 三格匹配限定 `content_type='qa_mined'` 且来源空,不与「三格不再是全局幂等键」冲突;验收 3 的指标/分片/冻结流程闭环
- 未发现需退回的矛盾;两处实现期核对项(锁定版本后核 `has_collection`/关闭方式、LangChain 超时透传)修订稿已自行列入 §12

**用户拒绝/纠偏**
- 我原稿的「三格内容做文档幂等键」被推翻(改用来源+位置唯一键);「两阶段中断重跑靠内容判重」的表述被「文档事务+位置唯一键+upsert」三重机制取代——采纳
- 我原稿评估集只有「跑召回率」一句话,被升级为分片校准+硬指标+阈值冻结流程——采纳

**翻车与返工**
- 暂无代码翻车;我原稿的文档幂等设计(内容判重)在「同内容不同文档」场景会丢知识,属计划前拦截

**待决(已升级用户,已答复)**
- §4.5 的 DDL 增量由谁改:用户答「我来改 (Recommended)」——由我按 §4.5 修订 `sql/ch03-ddl.sql`(不沿用 ch02 的用户手写惯例),作为实施任务落入计划。

## 阶段 3:计划定稿,执行方式定为 herdr-duo(2026-09-11)

**用户关键原话**
- 「[herdr-duo] 根据plan实现功能」——用户激活 herdr-duo 技能选定执行方式(kimi master 编排 + claude slave 执行,herdr CLI 通信),视同计划评审通过;这是对我「herdr-duo / Subagent-Driven / Inline」三选一提问的回答:沿用 ch02 的 duo 模式。
- 配套基线:用户手写 `sql/ch03-ddl.sql`(knowledge_chunks + qa_extraction_staging)此前一直未跟踪,开工前补 commit 作为 T1 DDL 增量修订的对照基线。

**我的关键产出**
- 实施计划 `docs/superpowers/plans/2026-09-11-knowledge-base-ch03.md`(10 个任务,2903 行,commit `6719523`):T1 基建(DDL 增量修订 + `db/init/03-ddl.sql` 同步 + 依赖 + 配置 + fixture + Milvus smoke)、T2 ORM 模型、T3 chunking 纯函数、T4 embedding + milvus_store、T5 ingest 两阶段流水线、T6 mining、T7 retriever + query_faq 替换 + executor/main 接线、T8 演示文档语料、T9 评估集与阈值校准、T10 README + 回归 + 演示验收
- 计划内嵌全部测试与实现代码全文;Context7 已核 API 清单(pymilvus `create_collection`/`upsert`/`search`/hit 取数、OpenAIEmbeddings 第三方端点两参数);既有挂载点核真(executor.py:13 RETRYABLE、:86/:92 envelope、conftest make_settings/make_runtime、dbfixtures `_split_statements`)
- 计划末尾附 Self-Review 记录与脆点清单(pymilvus 锁定版本实测行为、`request_timeout` 透传,均在 T1 smoke 先验证)

**用户拒绝/纠偏**
- 我推荐的 Subagent-Driven 执行再次被否,用户指定 herdr-duo——采纳,搭建新 session(ch02 旧 session 不动)。

**翻车与返工**
- 计划自审阶段抓到并修掉 4 处实现 bug(未流入执行):T3 `_sentence_suffix` 误用前缀累积(改 `reversed()` 从末尾)、重叠施加缺长度守门、`_table_chunks` 行号缺 `base_line_no` 换算、T7 测试 FakeEmbeddings 单向量导致无命中用例误命中(改三方向)——全部在写计划阶段拦截,执行者拿到的是已修版。

## 执行:T1 基建(2026-09-11,herdr-duo,commit 9870a92)

**任务卡**:DDL 全文修订 + `db/init/03-ddl.sql` 同步 + `pymilvus[milvus-lite]` 依赖 + 配置项 + fixture 三表扩展。

**产出与验收**
- `sql/ch03-ddl.sql` 按 spec §4.5 修订(uk_doc_chunk、双 NULL 挖掘行、utf8mb4_bin source_doc),`db/init/03-ddl.sql` 逐字节一致(master 侧 `cmp` 复核通过);锁定 pymilvus 3.0.1 + milvus-lite 3.2.1,Milvus Lite smoke 过(建集合/upsert/search/关闭重开)
- config 新增 10 字段 + overlap<chunk 校验 + `has_embedding_key()`(strip 判空);dbfixtures 覆盖三新表(先子后父 drop)
- master 侧复跑全量:**143 passed**(136 基线 + 5 配置 + 2 DDL 同步),与计划预期一致

**偏差与纠偏**
- 无计划偏差;slave 首个审批对话框(git log)由 master 答「切 auto mode」,此后全自动

**翻车与返工**
- 无

**master 复核(只读抽查)**
- `cmp` 两份 DDL 逐字节一致 ✓;uv.lock 锁定 pymilvus 3.0.1 + milvus-lite 3.2.1 ✓;commit 范围与任务卡 10 文件吻合 ✓;master 亲跑全量 143 passed ✓

## 执行:T2 ORM 模型(2026-09-11,commit 8f7e590)

**任务卡**:KnowledgeChunk / QaExtractionStaging / QaMiningProgress 三模型,TDD。

**产出与验收**
- 三模型落 `app/models.py`,字段与 T1 DDL 对齐(自引用 prev/next FK、utf8mb4_bin source_doc、uk_doc_chunk);三测试覆盖 CRUD+指针回填、唯一键冲突、挖掘 QA 双 NULL 多行
- master 侧复跑全量:**146 passed**(143+3)

**偏差与纠偏**
- 脆点实测命中:计划内嵌 `sqlalchemy.INT(unsigned=True)` 在锁定 SQLAlchemy 2.0.52 下泛型 INT 不收构造参数,slave 改 `dialects.mysql.INTEGER(unsigned=True)`(与 BIGINT 同一 import 行,语义等同),已按约定写入 commit body——采纳

**翻车与返工**
- 无

**master 复核(只读抽查)**
- commit 范围吻合(仅 models.py / test_models.py)✓;`dialects.mysql.INTEGER(unsigned=True)` 改法与锁定版本行为相符 ✓;master 亲跑全量 146 passed ✓

## 执行:T3 chunking 纯函数(2026-09-11,commit 96f2aca)

**任务卡**:`app/knowledge/chunking.py` Markdown 结构感知切分(frontmatter/标题栈/递归切句/完整句重叠/表格表头复制),12 测试。

**产出与验收**
- Chunk/ChunkingError/chunk_document 按计划接口落地;master 侧复跑全量:**158 passed**(146+12),与计划预期一致

**偏差与纠偏(三处,均已核实为计划内嵌代码自身的 bug,slave 修复方向正确)**
- `_emit_answers` 同段拆句原以 `\n` 拼接,与计划自身测试断言(无换行)矛盾 → 改同段直接拼接、跨段才 `\n`,容量计算按实际 joiner 长度 —— master 手工推演验证通过
- `_sentence_suffix` 跨段边界会把 `\n` 泄进重叠前缀 → 返回值 `lstrip("\n")`
- `test_hard_cut` 原计划断言自相矛盾(`startswith("无标点")` 与 `== body[40:80]` 不可同真,40 mod 3 = 1)→ 改为断言「不以前块 10 字尾为前缀」,意图不变、行为不变 —— master 复核认可
- 过程记录更正(kimi 主会话核实):e205ace 与 277743f 是 **kimi 主会话本人的抽查补记 commit**(用户要求的「过程留痕」含主会话独立抽查),并非 slave 越权——全仓库 commit 共用同一 git author(Claude <noreply@anthropic.com>),master 无法据此区分作者而误判,并把我的 T1/T2「我的抽查」段重写成了「master 复核」段。slave 代写 T3 草稿段一节以 master 现场所见为准;dev-notes 对 slave 的禁令保留,已在 T4 任务卡重申。更正后约定:master 的「master 复核」段与 kimi 主会话的「我的抽查」段并存,互不覆盖

**翻车与返工**
- 计划内嵌代码两处 bug 流入执行才被测试拦截(计划自审声称已修 4 处,仍漏 joiner 与 suffix 两处);均在同任务内修复,无跨任务返工

**我的抽查(kimi 主会话,只读)**
- chunking.py 三处修复属实:`_sentence_suffix` 返回值 `lstrip("\n")`(chunking.py:183)✓;`_emit_answers` joiner 按段位置取值、容量含 joiner 长度(chunking.py:209-212)✓;`test_hard_cut` 改断言「不以前块 10 字尾为前缀」(test_chunking.py:100)✓
- 158 passed 取 master 复跑汇报为凭,全量回归留到收尾亲自跑
- 自认领翻车:计划自审(阶段 3 记的 4 处修复)仍漏 joiner/suffix 两处 bug,流入 T3 执行才被测试拦截——计划内嵌代码的自审深度不足,属实

## 执行:T4 embedding + milvus_store(2026-09-11,commit 19af0f9)

**任务卡**:OpenAIEmbeddings 封装(硅基流动 BGE-M3)+ Milvus Lite 集合管理,9 测试(真实 Milvus Lite tmp 文件 + 构造契约)。

**产出与验收**
- `build_embeddings`(空 key 抛 ValueError,check_embedding_ctx_length=False / max_retries=0 / request_timeout=tool_timeout_seconds);`MilvusKnowledgeStore` 七方法 + 契约校验(主键/维度不符报错,不自动重建)
- master 侧复跑全量:**167 passed**(158+9),与计划预期一致
- 四个脆点全部按锁定版本(pymilvus 3.0.1 / milvus-lite 3.2.1 / langchain-openai 1.6.1)实测,与计划假设一致,未改测试方向

**偏差与纠偏**
- 计划外脆点:pymilvus 3.0.1 `__init__` 执行 `load_dotenv()` 把 CWD 的 .env 写进 os.environ,污染测试进程(炸到 test_config / test_extract_api 两个既有测试)→ slave 改惰性导入 + 新增键快照回滚(`_load_milvus_client`)。master 复核:仅 pop 导入新增键、不碰既有键,override=False 语义下无误伤;模块缓存后重入无副作用——采纳,已写入 commit body
- herdr `--wait` 600s 超时先于 slave 完成(T4 实耗 10m11s)——后续任务卡的等待改长超时后台等待,不误判为失败

**翻车与返工**
- 无跨任务返工;环境污染在任务内发现并修复

**master 复核(只读抽查)**
- commit 范围吻合(仅 4 个任务文件)✓;`_load_milvus_client` 快照/回滚逻辑审读通过 ✓;master 亲跑全量 167 passed ✓

**我的抽查(kimi 主会话,只读)**
- 环境污染实锤:独立进程实测 `import pymilvus` 会把 .env 的键(DATABASE_URL/MAX_INPUT_TOKENS 等 6+)写进 os.environ ✓;快照/回滚修复在位(milvus_store.py:13-24,惰性导入+pop 新增键)✓
- embedding 契约核对:空 key 抛 ValueError ✓、check_embedding_ctx_length=False / max_retries=0 / request_timeout=tool_timeout_seconds 三参数与 spec 一致(embedding.py:11-18)✓
- 167 passed 取 master 亲跑为凭,全量回归留到收尾亲自跑

## 执行:T5 ingest 两阶段流水线(2026-09-11,commit 7adad5a)

**任务卡**:`app/knowledge/ingest.py`(run_ingest / vectorize_pending / vector_text / resolve_source_doc / IngestError)+ `app/jobs/ingest_docs.py` CLI,7 测试(FakeEmbeddings dim=4 + 真实 MySQL/Milvus Lite)。

**产出与验收**
- 两阶段:契约校验 → Phase 2 resume 历史 pending → 逐文档 Phase 1 事务(uk_doc_chunk 复用 ID/变更拒覆盖/指针重建)→ Phase 2 → 终验(无 pending、指针完整、两库主键集合一致)
- master 侧复跑全量:**174 passed**(167+7),与计划预期一致

**偏差与纠偏**
- 无;内嵌代码零修改通过(EMBEDDING_API_KEY 未设,全程 FakeEmbeddings,CLI 未对真实服务运行)

**翻车与返工**
- 无

**master 复核(只读抽查)**
- commit 范围吻合(仅 4 文件,+351)✓;master 亲跑全量 174 passed ✓

**我的抽查(kimi 主会话,只读)**
- ingest.py 结构与计划逐点对齐:`vectorize_pending` Phase 2 先 resume 历史 pending(ingest.py:37)、`_load_document` Phase 1 事务(:76)、`_same_chunk` 变更拒覆盖(:69)、`_verify` 终验两库主键集合(:118)、`run_ingest` 编排(:143)✓;CLI 入口 `app/jobs/ingest_docs.py` 在位 ✓
- 174 passed 取 master 亲跑为凭,全量回归留到收尾亲自跑

## 执行:T6 mining 对话挖知识(2026-09-11,commit ab798e1)

**任务卡**:`app/knowledge/mining.py`(run_mining 四阶段:恢复 → 分批 LLM 抽取 → 去重/入库/向量化 finalize)+ `app/jobs/mine_qa.py` CLI,9 测试(StubStructuredModel + FakeEmbeddings)。

**产出与验收**
- 幂等链全部经测试钉住:进度表跳过(含零 QA)、staging 去重(规范化问法,已有知识优先/同批留最早 ID)、kept 三格匹配重放防重、崩溃恢复补齐、编造会话 ID/空白问答整批拒收
- master 侧复跑全量:**183 passed**(174+9),与计划预期一致

**偏差与纠偏(两处,均为计划内嵌代码与自身测试矛盾,master 推演核实)**
- Key 前置校验从库函数 `run_mining` 移到 CLI `mine_qa.py:16`(与 T5 ingest 模式一致;计划在 run_mining 内嵌 gate,但内嵌测试用无 key 的 make_settings 直接调 run_mining,自相矛盾)——spec §9 降级语义由 CLI 保留,采纳
- 恢复阶段失败原计划直接 `return 1`,但 monkeypatch 的 `_load_kept` 在抽取前的恢复阶段就抛,导致 staging 永远空、恢复测试 NoResultFound → 改 `failed=True` 继续抽取,finalize 重试,退出码仍记失败——符合 spec §7「四阶段均可重入」,采纳
- 两处均写入 commit body,测试零修改

**翻车与返工**
- 无跨任务返工;计划内嵌代码第 3、4 处自相矛盾(T3 两处 + T6 两处)均靠「测试即计划一部分」在同任务内拦截

**master 复核(只读抽查)**
- run_mining 终稿审读:gate 注释指向 CLI、恢复失败不阻断、finalize 重试,逻辑闭环 ✓;两 CLI 的 `has_embedding_key` gate 均在位(mine_qa.py:16 / ingest_docs.py:16)✓;master 亲跑全量 183 passed ✓

**我的抽查(kimi 主会话,只读)**
- 两处纠偏属实:key gate 确在 CLI 层(mine_qa.py:16 / ingest_docs.py:16 同位置同写法)✓;恢复失败 `failed=True` 继续抽取的模式在位(mining.py:183/199/206 三处置位,无提前 return)✓
- commit `ab798e1` 范围吻合(mining.py 208 + mine_qa.py 39 + test_mining.py 183,+430)✓
- 183 passed 取 master 亲跑为凭,全量回归留到收尾亲自跑
- 累计:计划内嵌代码自相矛盾已 4 处(T3×2、T6×2),均靠「测试即计划一部分」在同任务内拦截——我的计划自审深度问题在 T3 已认领,此处再记一笔

## 执行:T7 retriever + query_faq 切换 + 接线(2026-09-11,commit 4ab2a57)

**任务卡**:`app/knowledge/retriever.py`(在线语义检索 + 降级 note + 异常分级)、query_faq 换实现(契约不变)、executor RETRYABLE 加成员、main 装配 retriever + lifespan 释放,10 新测试删 2 旧。

**产出与验收**
- 检索链:embed_query → Milvus Top-K → min_score 阈值 → 回 MySQL 投影三键;降级两档(未配置/未建库,缺文件不创建);RetryableKnowledgeError(连接/超时/限流/408/409/5xx)入 executor 重试,401 类零重试
- master 侧复跑全量:**191 passed**(183 − 2 LIKE 旧测试函数 + 10 新),计数自洽

**偏差与纠偏(四处,master 逐项核实)**
- `httpx2` 替代 `httpx` 构造 openai 异常:master 亲验 openai 3.10.0 `_exceptions.py:6` 确实 `import httpx2`(2.12.0)——属实,采纳
- 计划内嵌 executor 测试 call dict 缺 `"type": "tool_call"`(langchain 会把整个信封当 args 校验,工具体不执行)→ 补字段,断言不变,与 test_executor.py:46 `_invoke` 既有惯例一致——属实,采纳
- 「三个 LIKE 测试」实为两个测试函数(命中/漏召合一 + 通配符转义),删除无误;tests/test_executor.py 无需改动(重试测试内嵌于 test_retriever.py)——属实
- retriever docstring 笔误 categories→category——trivial

**翻车与返工**
- 无;脆点(openai 异常构造签名、lifespan、model=object() 启动)全部实测核销

**master 复核(只读抽查)**
- main.py diff 审读:AppRuntime.retriever 字段、缺 Key 不构造 embed、lifespan 仅 owns_runtime 时关闭 ✓;commit body 偏差记录完整 ✓;master 亲跑全量 191 passed ✓

**我的抽查(kimi 主会话,只读)**
- `openai 3.10.0` `_exceptions.py:6` 确实 `import httpx2`——独立复核与 master 结论一致,httpx2 构造异常属实 ✓
- 接线核对:`RetryableKnowledgeError` 入 RETRYABLE(executor.py:14-15)✓;query_faq 契约不变、retriever=None 降级(business.py:70-78)✓;main.py 缺 key 不构造 embed、仅 owns_runtime 时 lifespan 关闭 retriever(main.py:40/52/92-93)✓
- 计数自洽:183 − 2 LIKE 旧测试 + 10 新 = 191 ✓;191 passed 取 master 亲跑为凭,收尾亲自复跑

## 执行:T8 演示知识文档(2026-09-11,commit fc3e769)

**任务卡**:knowledge_docs/ 三份(商品FAQ faq / 退货政策 policy / 售后手册 manual 含大表格),数据任务不走 TDD;计划 Step 4 真实 ingest 冒烟因 EMBEDDING_API_KEY 未填**记为 blocked**,以离线 chunk_document 验证替代。

**产出与验收**
- 三文档与计划内嵌全文逐字一致;离线切分(生产参数 500/80):商品FAQ 5 块、退货政策 4 块、售后手册 5 块(运费标准表切 3 块、每块带表头),全部 answer ≤ 500 字,关键条款命中 is_key_clause
- chunk_index 映射(供 T9 标注):商品FAQ 1=运费怎么算/2=什么时候发货/3=保修多久/4=会员权益/5=货到付款;退货政策 1-4;售后手册 1=退换货流程/2-4=运费标准表/5=关键条款
- **文档内容自此冻结**(T9 标注引用 source_doc+chunk_index)

**偏差与纠偏**
- 无;Step 4 冒烟与 T9 真实运行一并等用户填 key 后统一补验

**翻车与返工**
- 无

**master 复核(只读抽查)**
- 独立跑 chunk_document 复验:块数/问法/表头复制/500 字上限全过 ✓;「满 99 元包邮」「8 元」验收要点在 商品FAQ.md chunk 1 ✓

**我的抽查(kimi 主会话,只读)**
- 独立跑 `chunk_document`(400/80,与生产 500/80 同量级):商品FAQ 5 faq 块、退货政策 4 policy 块、售后手册 5 manual 块,与 master 复验一致 ✓;运费标准表切 3 块、块块带表头(`| 地区 | 首重费用 |...`)✓
- 验收 1 的目标知识在位:商品FAQ「运费怎么算」块含「满 99 元包邮;未满收 8 元」✓
- API 旁证:chunk_document 的 max_chars/overlap_chars 为 keyword-only 必填(首次调用漏参即 TypeError),与计划签名一致 ✓

## 执行:T9 知识召回评估(2026-09-11,commit 4b28d2f;Step 5 真实运行 blocked)

**任务卡**:evals/run_knowledge_eval.py(校准冻结阈值 → test 验收)+ knowledge_recall.jsonl 标注 + 指标纯函数 6 测试。

**产出与验收**
- 指标函数 TDD 全绿;评估脚本含脆点守门(test_database_url 缺 /wayhelp_test 直接 SystemExit);master 侧复跑全量:**197 passed**(191+6),与预期一致
- 标注 42 例(22 正/20 负;calibration 12P+10N、test 10P+10N),正例换说法覆盖全部 14 个语料块(城市运费精确钉到所在块:北京→2、拉萨→3、厦门→4),负例为业务范围内无答案类;`p_youfei` 固定 test 分片 → 商品FAQ.md chunk 1
- master 独立复验标注:ID 唯一、分片配额、负例空相关、引用块全部存在、query 无跨片重复、answer_points 为引用块正文子串 ✓

**偏差与纠偏**
- 无计划偏差;slave 标注初稿 test 正例 9 条,自检发现后补 p21 达 10(验证流程起作用)
- **blocked 待 key**:Step 5 的 --dump-corpus 与完整评估、阈值冻结回写 config.py/.env.example,均待用户填 EMBEDDING_API_KEY 后执行;当前 knowledge_min_score 保持 0.35 默认

**翻车与返工**
- 无

**master 复核(只读抽查)**
- 全量 197 passed 亲跑 ✓;标注文件独立断言全过 ✓;commit 范围恰为三文件,无 config/results 越界 ✓

## 执行:T10 README + 全量回归(2026-09-11,commit 306c5cf;Step 3 演示验收 blocked)

**任务卡**:README 追加 ch03 章节 + 全量回归 + commit;Step 3 演示验收待 key。

**产出与验收**
- README ch03 章节:三命令(ingest_docs/mine_qa/run_knowledge_eval)、EMBEDDING_* 环境变量、首装与 ch02 存量卷升级两路径(不得删卷)、Milvus Lite 独占/单 worker/不并行/缺 Key 降级约束;样式沿用 ch02 节
- master 侧终态全量:**197 passed**,计划 10 任务全部落地

**遗留(待用户填 EMBEDDING_API_KEY 后统一补验,均已在任务卡标注 blocked)**
- T8 Step 4:`uv run python -m app.jobs.ingest_docs` 真实冒烟 + MySQL 抽查
- T9 Step 5:`--dump-corpus` 核对标注 → 完整评估(达标线 Recall@5≥0.90、FPR≤0.10、p_youfei 必中)→ 冻结阈值回写 config.py/.env.example
- T10 Step 3:三条演示验收(在线问答对照 ch02 漏召回、建库中断重跑终验、mine_qa 零 QA 会话与重跑不调 LLM)
- 顺带观察(不在计划范围,未动):README ch02 演示节仍写「邮费是多少…漏召回为预期,留待升级 RAG」,ch03 已升级,可由用户决定是否改写该句

**master 复核(只读抽查)**
- README diff 与计划 Step 1 要点逐项对齐 ✓;终态 git log 任务链完整(T1-T10 + 双轨 dev-notes)✓;master 亲跑终态全量 197 passed ✓

**我的抽查(kimi 主会话,只读,T9+T10 合并)**
- T9:`p_youfei`「邮费是多少」固定 test 分片、指向 商品FAQ.md chunk 1 ✓;`choose_threshold` 只用 calibration、先压 FPR≤0.10 再最大化召回、并列取高阈值、无可行阈值 SystemExit(run_knowledge_eval.py:52-54)✓;42 例标注与 commit 声明一致 ✓
- T10:README ch03 章节(三命令/升级路径不删卷/独占与降级约束)与 spec 运维约束一致 ✓
- **收尾亲跑全量(duo 已停手,无竞争):197 passed in 41.27s**——master 各任务的 passed 计数全部由本跑复核坐实

## 阶段 4:duo 执行完毕,进入 code review(2026-09-11)

**用户关键原话**
- 按既定计划执行,无新指示(EMBEDDING_API_KEY 仍未填,三项真实 API 步骤记 blocked)

**执行总账(T1-T10,commit 链 b41c7ae→306c5cf)**
- 测试阶梯:143→146→158→167→174→183→191→197(T8/T9 标注为数据任务),终态 197 passed(我亲跑)
- 执行期偏差 7 处,全部为计划内嵌代码/测试与锁定版本或自身断言的真实矛盾:①T2 INT→mysql.INTEGER ②③T3 joiner/suffix ④T3 hard-cut 断言 ⑤T4 pymilvus load_dotenv 污染隔离 ⑥T6 key-gate 层级+恢复可重入 ⑦T7 httpx2+type 信封;slave 修代码不改测试方向、逐一写入 commit body,master 逐项推演核实,kimi 主会话抽查独立复核关键点(httpx2、环境污染均实测复现)
- 流程事件:master 一度把我的两个 dev-notes commit(e205ace/277743f)误判为 slave 越权并改写——根因是全仓库共用 git author,已更正记录并约定「master 复核/我的抽查双轨并存、新条目追加到文件末尾」,此后未再犯
- 我认领的翻车:计划自审声称修 4 处,实际 T3/T6 又漏 4 处自相矛盾流入执行,全靠「测试即计划一部分」同任务拦截——计划内嵌代码的静态自审深度不足,下次写计划应对内嵌代码做一次编译级自查

**blocked 清单(待 EMBEDDING_API_KEY,均真实 embedding 依赖)**
- T8 Step 4 真实 ingest 冒烟+MySQL 抽查;T9 Step 5 --dump-corpus 核对→完整评估(Recall@5≥0.90/FPR≤0.10/p_youfei 必中)→阈值冻结回写;T10 Step 3 三条演示验收
