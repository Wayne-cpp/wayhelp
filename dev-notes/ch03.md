# Ch03 开发过程记录 —— RAG 基础:知识库与向量语义检索

## 阶段 1:Brainstorm 定稿(2026-09-11)

**用户关键原话**
- 「我要用 Superpowers 模式给客服系统建知识库,把 query_faq 的内部实现从关键词查表升级成向量语义检索,工具的入参出参契约保持不变。」
- 「技术栈:嵌入模型 BGE-M3;向量库 Milvus,MySQL 当原文权威源」「本章不做:关键词召回、混合检索、重排,本章只跑 dense 向量单路」
- 「涉及具体库、框架、API 的用法,一律先用 Context7 MCP 查最新官方文档和接口定义再动手,别凭记忆写」
- 「上面点名的技术选型是定死的,实现中发现矛盾或走不通,停下来问我,不要自行换方案」
- 「[ch03-ddl.sql] 是本次需要建表的sql」——knowledge_chunks + qa_extraction_staging 两张表 DDL 由用户手写提供(沿用 ch02 惯例)
- 「产出不是可单测代码的任务(纯 Prompt、数据类),把 TDD 那步换成拿标注样例或评估集跑一遍验证」
- 「过程留痕:每完成一个阶段就补一段,记四样……不许收尾时一次性补记」

**澄清问答(AskUserQuestion 五轮)**
- 嵌入接入:用户答「用硅基流动的,token后面我填进去」——**远程 Embedding API**(OpenAIEmbeddings 指硅基流动,model=BAAI/bge-m3),否掉我推荐的本地 sentence-transformers(2.3GB 模型下载)
- Milvus 部署:用户答「用 Milvus Lite 嵌入式」——否掉我推荐的 Docker Compose standalone(与 ch02「测试真实基础设施同构」惯例有出入,但用户拍板本地文件;测试改用 Milvus Lite tmp 文件,仍是真实向量库)
- 数据来源:用户选「都由我造演示数据」——演示 Markdown 文档我写入仓库;历史对话直接挖现有 MySQL conversations/messages 表
- 任务形态:用户答「弄个命令可以启动job就可以,我可以直接选择部署或直接执行」——CLI 命令(ingest_docs / mine_qa),不内嵌 APScheduler
- 方案选择:用户选 **B(裸 pymilvus + OpenAIEmbeddings)**,否掉 A(langchain-milvus 全家桶)、C(自抽 VectorStore 协议)

**我的关键产出**
- 路径分类:architectural(新增知识库子系统:离线建库 + 挖矿 + 在线检索替换)
- 三段设计,逐段用户确认(§1 模块结构+§2 文档切分 → §3 双写+§4 挖知识 → §5 检索+§6 错误处理+§7 测试验收,三次「ok」)
- spec:`docs/superpowers/specs/2026-09-11-knowledge-base-ch03-design.md`(12 节)
- 关键设计点:Milvus 集合只存 id+vector(原文回 MySQL 取,权威源单一);Phase 1 load / Phase 2 vectorize 两阶段,中断重跑天然成立;Milvus 写入用 upsert 兜底「Milvus 已写、MySQL 未回填」的中断缝隙;挖矿 source_ref 判重实现「同一会话只挖一次」;去重为规范化文本确定性匹配,不引第三个模型
- Context7 核对(动手前):pymilvus MilvusClient/create_collection/upsert/search 签名;OpenAIEmbeddings 构造与 embed_query/embed_documents;**抓到真坑**:第三方模型必须 `check_embedding_ctx_length=False` 跳过 tiktoken,已写入 spec §2/§12

**用户拒绝/纠偏**
- 嵌入接入否本地库、Milvus 否 Docker standalone(均见上);方案 A/C 被否
- 三段设计确认均无修改,一次通过

**翻车与返工**
- 暂无(设计阶段未动代码)。
