# ch06 开发实录 — 正式版分流器

## 2026-09-19 brainstorm 定稿

**我的关键原话（需求定死项）**：「指代消解……问题本身已完整、指代已明确的，原样透传，不强行改写」「Query 改写和指代消解一并处理」「扩写只对退款退货、售后这类高频、容错低的核心场景做，简单 FAQ 不扩；扩写放检索侧现查现用，库里知识只留一份」「意图识别用 LLM prompt 路线，不训分类模型」「强制 JSON 输出，字段就 intent 和 confidence 两个」「留『其他』兜底类，拿不准就归它」「缺订单号不让模型猜，执行阶段弹订单选择器让用户点选回填；退款原因不追问……从固定类目里自选；需求澄清留在主力 Agent 里做」「默认配够格的大模型先把准确率做上去，成本吃紧时再走降级路」「前端部分是例外，用 Vibe Coding 方式直接做」。

**关键产出**:`docs/superpowers/specs/2026-09-19-ch06-router-design.md`(brainstorm 探索由三个 explore subagent 完成：graph/SSE、检索/工具/evals、前端交互）。设计决策 D1-D10 全部经我逐题确认。

**我拒绝/纠偏了什么**:
- 订单数据：否了「建真实订单表」，选确定性 mock 订单服务（不碰 DDL 红线）。
- 缺订单号回传：**否了 agent 推荐的「点选=发新消息」**，定 LangGraph interrupt/resume，图从挂起点继续。
- 意图 JSON：确认删 needs_knowledge 维度，路由改纯 intent 映射（现 14 组合路由表作废）。
- 退款单：否了独立建表和纯前端模拟，复用 tickets 表 + /v1/chat/action 扩展。
- 意图模型：否了本章实现 cascade，只大模型 + 配置项。
- 拓扑：选了方案 A 顺向流水线（否了理解层大合并 B、独立子图 C)。

**翻车与返工**：暂无。agent 在 §1 设计里承诺「挂起期间发新消息覆盖挂起态」时如实标注：LangGraph 对「pending interrupt 时传新 dict」的精确语义官方文档无逐字说明，已用 Context7 核实 interrupt/resume 其余语义（节点从头重跑、resume 值回传、aget_state 查 pending)，新输入覆盖语义留待实现期写探测测试钉死。

## 2026-09-19 spec 评审修订（GPT 评审 + agent 实测复核）

**我的关键原话**:「我已经使用gpt6进行review并修改」；方向确认题回答「接受，按修订版走」。

**关键产出**:spec 工作区修订（未提交时 git diff 全文复核），主要变化：商品咨询 knowledge→business + 图专用只读 query_faq(原 knowledge 顶层出口取消)、新增 refund_scope 节点(general/order_specific/clarify)、订单卡绑定 interrupt ID 且 resume 走 {id: value} 映射、active_order 跨轮字段、多查询候选只按 chunk_id 合并后统一重排（不混策略分数)、messages 流加 chat_visible tag 过滤、understand 历史按完整轮计、订单服务全链路统一 get_order(user_id, order_id) 归属校验。agent 修正了 §2 一处不精确描述后提交。

**实测复核（agent 在 .venv langgraph 1.2.11 + InMemorySaver 跑探针）**:
- ✅ 挂起时传新 dict → 从 START 新轮、产生新 interrupt ID;裸 resume 值作用于当前 pending；与 spec 所述一致。
- ⚠️ 补充：不匹配的旧 interrupt ID **被静默忽略**(LangGraph 不报错不执行)——spec §9.1 的「服务端锁内自校验 409」由此证实为硬性必要，不是防御冗余。
- ⚠️ 纠偏：§2 原写「对已完成的图重复 resume 可得到空流」，实测非空（回放当前最终 state chunk)，已改为准确描述。
- ✅ 代码事实：app/tools/business.py:120 的旧 build_tools 确实捆绑 query_faq+create_ticket（写工具）,spec 禁止复用它恢复 query_faq 的警告属实。

**我拒绝/纠偏了什么**：无新增否决；对「① 商品咨询改 Agent 按需检索、库外拒答从图闸变工具+提示约束 ② 扩写收窄 order_specific + 检索 scope=None 全库（原话「强制检索政策条款」被评审扩大）」两处方向性变化，经 agent 点名提示后明确接受。

**翻车与返工**：无。spec 进入待写 plan 状态。

## 2026-09-19 实现计划落盘(writing-plans)

**我的关键原话**:（无新指令——流程自然推进到 writing-plans;早前定死的纪律:「涉及具体库、框架、API 的用法一律先用 Context7 MCP 查最新官方文档再动手」)

**关键产出**:`docs/superpowers/plans/2026-09-19-ch06-router.md`(13 个 Task,含 Global Constraints 与 File Structure)。写作前 agent 通读 spec 终稿与 14 个核心源码文件,并用掉第 3 次 Context7 查询核实 messages 流 `metadata["tags"]` 过滤(per-call config 传 tag 为官方方式)。

**计划关键裁决**:
- Task 2(意图契约两字段化)把 refund 出口**临时**接旧 retrieve 链,Task 7 再切正式子流程——避免级联断裂,每任务结束全量绿。
- Self-Review 抓出 6 处缺口已修进计划:FakeStreamModel.astream 签名不接 config、a11 意图罐头会落 gate_fallback、test_refund_flow 脚本顺序(suggest 须在最终答复前)、tag 过滤缺集成钉+with_config fallback 注记、缺 SQLite 重开/新消息覆盖挂起/并发 resume 三个集成钉。
- 挂起轮不落 messages → 下一轮仍是「无历史」首轮、understand 跳过的脚本消费序不确定性,已在计划中标注「实现第一天用测试钉死」。

**我拒绝/纠偏了什么**:无。
**翻车与返工**:无(计划阶段)。

## 2026-09-19 执行方式定夺 + herdr-duo 开工

**我的关键原话**:agent 弹窗问执行方式(Subagent-Driven / Inline)时我未选择直接关闭;随后用 slash 触发 `/herdr-duo 根据plan实现计划`——即选定 herdr master/slave 双人组执行计划。

**关键产出**:`herdr-duo.sh` 在命名会话 `dev-wayhelp` 起好 master(kimi)/slave(claude) 双 pane 并各自 idle;确认 Docker MySQL 在线(测试硬依赖)后,把计划文件 `docs/superpowers/plans/2026-09-19-ch06-router.md` 连同仓库纪律(全量 443 基线、Conventional Commits、每任务 dev-notes 追记、Task 11 前端 Vibe 例外、计划与现实脱节先改计划)一次性交给 master,后台 `--wait` 守候。master 按 briefing 内建的串行派工环执行:一次一派、按任务验收、过才派下一个、任务间 /clear slave 上下文。

**我拒绝/纠偏了什么**:执行方式选择题被我关闭未答,最终用 herdr-duo slash 直接给出第三种答案——计划执行权整体外包给 duo,agent 本人转监理。

**翻车与返工**:暂无。风险备录:13 任务含多次全量 pytest,预计小时级;若 herdr `--wait` 超时(2h 上限),用 `herdr agent wait master` 续等而非重复派 prompt。
