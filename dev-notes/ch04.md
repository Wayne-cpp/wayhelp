# Ch04 开发过程记录 —— RAG 进阶:混合检索 + 重排 + 生成质量控制 + 评估体系

## 阶段 1:Brainstorm 定稿(2026-09-15)

**用户关键原话**
- 「我要用 Superpowers 模式把客服系统的检索质量做上一个台阶:混合检索加重排,再配一套评估体系。」
- 「关键词召回这条路也落在 Milvus 上,用它 2.5 版起原生的 BM25 全文检索,text 字段挂 BM25 函数,中文分词用内置 chinese analyzer」「重排模型 bge-reranker-v2-m3」——技术选型定死
- 「涉及具体库、框架、API 的用法,一律先用 Context7 MCP 查最新官方文档和接口定义再动手,别凭记忆写」
- 「实现中发现矛盾或走不通,停下来问我,不要自行换方案」
- 「前端部分是例外,用 Vibe Coding 方式直接做」
- 「产出不是可单测代码的任务(纯 Prompt、数据类),把 TDD 那步换成拿标注样例或评估集跑一遍验证」
- 「[sql/ch04-ddl.sql] 这是建表语句」——low_confidence_questions + faith_cases 两张表 DDL 用户手写提供(沿用惯例)
- 「[retrieval_compare.txt] 这个是评估集」——300 条 CSV 评估集用户提供(A/B/C/E 各 60 + D_absent 拒答 60)
- 「我已经提供了,在 knowledge_docs 下,其他旧的都删除了」——新四份 md(喵喵优选宠物用品域)用户自备,旧三份已删

**澄清问答(AskUserQuestion 九轮)**
- Rerank 接入:用户选「硅基流动远程 API」——否掉本地 FlagEmbedding(~2GB 权重,与 ch03 否本地模型裁决一致)和独立推理服务
- 部署与迁移:用户三连反问(「是变 Standalone 吗」→「还是没看懂」→「为什么要动重建路径,不是用 milvus standalone 吗」→「重建后还需要用到 mysql 吗」)——暴露用户以为 BM25 必须上 Milvus Standalone;我用锁定版 milvus-lite 3.2.1 README 实测事实(纯 Python 重写代已支持 BM25/sparse/hybrid/RRF/Jieba)解释「不用换部署,重建的是集合 schema」+「MySQL 账本/Milvus 货架」分工,用户最终选「Lite 同名重建」
- 评估集组织:用户答「我会提供评估集,你告诉我路径」——我出格式与路径,用户随后直接给了 CSV 成品
- 元数据过滤:用户选「query_faq 加可选 category 参数」——否掉 query 理解模块统一产出、前端选择器
- 入池范围:用户选「两种拒答都入池」——**推翻其自写 DDL 注释**「本章只靠 useful 自评入池」,DDL 注释待修订(结构不动)
- 台账界面:用户选「本章只落表,处置用 SQL」——否掉 /kb 处置界面(DDL 注释里「人工点按钮改」留后续章节)
- 改写模型:用户选「复用主对话模型」——否掉独立模型配置
- 集成方案:用户选「retriever 内部升级」——否掉 HybridRetriever 并存、LangChain 全家桶
- 文档 frontmatter:用户选「我给四份文档补 frontmatter」——否掉放宽 chunking 契约
- 旧块清理:用户选「重建作业=全量重置」——挖掘块一并清(旧 FAQ 语境挖的,留着与新库矛盾)

**我的关键产出**
- 路径分类:architectural(检索/重排/Query 理解/生成控制/评估/前端六块 + 两张新表 + 评估基建)
- 动手前事实核查:Context7 核 pymilvus Function/BM25/add_function/hybrid_search/AnnSearchRequest/RRFRanker 签名;milvus-lite README 核实 Lite 3.x 支持 BM25(反驳了 2024-12 官方博客「Lite 不支持全文检索」的过时结论,系版本代际差异);**抓到用户选型与 Lite 的口径差**:「内置 chinese analyzer」在 Lite 上的等价物是 Jieba 分词器,参数形态列入脆点 C1
- 用户评估集核对:CSV 引号格式、五桶、`|`/`+` 期望章节规则、章节名与新文档标题对得上(如「MH-LP100」);D_absent 桶把拒答率纳入考核;发现新文档缺 ch03 必填 frontmatter、MySQL 存旧库脏块两个实施前置问题,均已裁决
- spec:`docs/superpowers/specs/2026-09-15-hybrid-retrieval-ch04-design.md`(14 节,commit `64ab607`),含证据组装「首尾排位」[1,3,5,7,9,10,8,6,4,2]、拒答双闸门与 source 判定矩阵、四策略评估指标(Recall@5/10 + MRR@10 + Faithfulness + 拒答正确率/误拒率)、faith_cases upsert 复发语义、Context7 必查清单与脆点 C1-C4
- 设计分三段呈现(§1-2 → §3-4 → §5-6),三次「ok」通过;git 基线:spec + 评估集 `64ab607`,知识文档换血 + ch04 DDL 草稿 `81e66b3`

**用户拒绝/纠偏**
- 本地 rerank 推理、Standalone 切换、HybridRetriever 并存、独立改写模型、台账界面、旧文档并存——均被否(见上)
- 用户自备的 DDL 注释(「只靠 useful 自评入池」)被用户本人口头推翻,以「两种都入池」为准

**翻车与返工**
- 暂无代码翻车;沟通层面:我对「重建」的初次解释用了太多术语(集合/schema/expr),用户连续三问才对齐——教训:对非专职 DB 用户,先打「表格结构/货架」比方再上术语

## 阶段 2:Spec 复审修订(2026-09-15)

**用户关键原话**
- 「spec我已经使用gpt6 review并修改了」——spec 经 GPT-6 复审修订(141 增 58 删,替我决定稿),沿用 ch03 惯例我逐项核真后并入(commit `7e2fd9f`)

**复审主要修订(GPT-6 稿并入,逐项核真)**
- 纠偏我原稿的「category 过滤」:现有 chunk.category 实为章节分类(如「商品规格手册」)而非商品品类;改为新增 `scope` 列(faq/policy/product_spec/after_sales_manual/qa_mined/manual 六枚举,content_type+source_doc 单一纯函数派生),过滤强制 `expr_params` 参数化防注入——**属实**,chunking.py 的 category 确为标题栈派生
- 语料缺口实测复现:补 frontmatter 后切出 **43 块(与修订稿完全一致)**;正例 GT 未覆盖 case 我算出 **24 条**(修订稿称「至少 17」,方向正确且保守),缺积分/可开票类型/开票时效/猫窝机洗/运费与包邮等章节——§11 语料补齐为真需求,「正文一字不动」约束作废
- D3 冲突属实:D3「能不能开纸质发票邮寄给我」(should_refuse=是)与 A30/E46 期望的企业纸质专票知识在语料补齐后必冲突;已按批准把 D3 改为「发票能不能用外币金额开具」
- 拒答升级确定性:检索硬闸门命中时**不发起第二次模型调用**,ChatService 直接输出固定拒答话术;生成侧自评改「去空白后与 REFUSAL_ANSWER 常量完全相等」才算,弃用我的宽松前缀匹配
- 入池原子化:commit_turn 扩展为 messages + low_confidence 同事务提交;系统故障(重建中/未配置/超时)明确**不得**伪装成 low confidence 入池
- 引用一致性:TurnToolset/RetrievalTrace 显式接口;唯一证据列表三处(tool 结果/citations SSE/envelope v2 metadata)逐项逐字段相等;每轮强制最多一次 query_faq;不推空 citations 帧
- 评估升级:固定奇偶分片(每桶奇 30 calibration/偶 30 test);新增 SectionRecall@K(AND 组覆盖率)/CompleteHit@K;四策略各自独立校准阈值(Pareto 候选 + 用户确认 D 桶误通过率约束,并列取误拒率低、阈值小);judge 结构化输出,失败记 judge_error 脚本非零退出;faith_cases 写主业务库跨轮累计;D3 修正纳入契约
- 工程硬约束:query_faq 独立 20s 总预算(monotonic deadline 全程共享,executor 零整链重试);rerank 内部 0.5s/1s 退避最多 3 次请求、耗尽降级 hybrid 并自动换用 HYBRID_MIN_SCORE;KnowledgeState 三态(ready/rebuilding/rebuild_required)+ 重建只读预检 + 维护状态;启动时只读校验两张 ch04 表存在

**我的关键产出(核真)**
- 引用点全部在位:commit_turn(sessions.py:20)、_tool_calls_legal(chat_service.py:284)、store_db.py/tool_envelope.py/schemas.py、job_busy(kb_admin.py:55);KnowledgeState 为新组件待建
- 奇偶分片数学自洽:五桶各 60 条,奇偶各 30;E27 的「猫窝」组也在未覆盖清单内,将由猫窝机洗新章节覆盖
- 三处动用户拍板项/用户文件,已停下问并获批准:scope 替代 category(接受)、D3 文案替换(接受)、语料由我补写 + validator 验收(接受)

**用户拒绝/纠偏**
- 我原稿「query_faq 出参带 section_path」被 GPT-6 改为「唯一 Evidence 列表三处相等、section_path 进 Evidence 但不单独投影」——采纳
- 我原稿「前缀匹配识别拒答」被收紧为精确相等——采纳
- 我原稿「rerank 走 executor RETRYABLE」被改为 reranker 内部重试 + 结构化降级,不进整链重试——采纳(避免叠乘)

**翻车与返工**
- 暂无;修订稿暴露我原稿两个语义糊涂处(category≠品类、拒答识别过宽),属计划前拦截

## 阶段 3:计划定稿(2026-09-15)

**用户关键原话**
- (spec 复审后无新指示,直接进 writing-plans)

**我的关键产出**
- 计划前脆点实测(替代纸面假设,全部锁定版本下亲跑):
  - C1 `analyzer_params={"tokenizer":"jieba"}` 建集合成;**JiebaAnalyzer 需 jieba 包** → 补依赖 jieba 0.42.1(uv add,推翻 spec「不新增第三方依赖」一句,已记 Global Constraints)
  - C2 BM25 查询 `data=[文本]` 直传命中型号文本 ✓;hybrid_search + RRFRanker(k=60) ✓,**RRF 分数量级 ~0.03**(阈值必须校准,严禁拍脑袋默认)
  - **翻车点实录**:`uv run` 自动加载 .env 使 pymilvus import 即炸 Illegal uri(项目 `_load_milvus_client` 隔离器就是为此存在,smoke 复用之即过);milvus-lite **不支持 expr_params/$占位符**(实测报错「only '$meta' is supported」)→ scope 过滤改枚举白名单+字面插值等效防注入,记为 spec §4.1 措辞的实测偏差;**run_analyzer UNIMPLEMENTED** → 改用 insert+search smoke 验证分词;重开客户端集合 released(既有 _ensure_loaded 坑,再次坐实)
  - 硅基流动 /v1/rerank 官方文档核签名:POST {model,query,documents,top_n} → results[{index,relevance_score}],用 index 回映射,不需 return_documents
  - httpx 在依赖树但位于 dev 组 → 计划 T1 提为主依赖
- 实施计划 `docs/superpowers/plans/2026-09-15-hybrid-retrieval-ch04.md`(2763 行,14 任务,commit `e027203`):T1 基建/T2 ORM+同事务入池/T3 五列 schema/T4 Query 理解/T5 reranker/T6 retriever 管道/T7 工具层+envelope v2/T8 提示词/T9 ChatService 双闸门/T10 语料补录+validator/T11 重建作业+装配/T12 评估脚本/T13 前端 vibe/T14 README+验收
- 计划自审抓到并修掉 5 处:T6 证据组装占位残留行、未用变量 empty、hybrid leg_counts 语义造假(改 fused 记法)、T9 second_skipped 与 else 互相覆盖(重排为 calls 内分流)、T2 source 类型标注与实现不一致、faith 测试同义反复断言——**ch03「计划内嵌代码编译级自查」教训首次主动执行**

**用户拒绝/纠偏**
- 暂无(待评审)

**翻车与返工**
- 计划前 smoke 翻车 2 次(env 污染、released 集合),均为已知坑的再次验证,未流入计划

## 阶段 4:执行启动——herdr-duo 接管(2026-09-15)

**用户关键原话**
- 「[herdr-duo] 根据plan实现计划」——执行方式三选一弹窗被关闭后,用户直接激活 herdr-duo 技能拍板:双代理(claude 执行 / kimi 复核)跑计划

**我的关键产出**
- herdr session `dev-wayhelp` 搭起:master(kimi,w1:p1)+ slave(claude,w1:p2),均 idle,工作目录即仓库根
- 已把计划 `docs/superpowers/plans/2026-09-15-hybrid-retrieval-ch04.md` 交给 master,附带六条执行纪律:定死选型不自换、库 API 先查官方文档、每任务真跑验证命令(基线 209 passed)、T13 走 vibe 例外、每任务验收后 Conventional Commits 提交、spec 优先;要求最终总结逐任务报完成情况/验证结果/翻车返工
- 巡检 cron 已挂(id `01M2JD6D4FC5AJMQPEZQVM57JT`,每小时 9/29/49 分):按 git log + master 输出逐任务追记本文件,收尾时删除

**用户拒绝/纠偏**
- 执行方式弹窗(Subagent-Driven 推荐/herdr-duo/Inline)用户未答直接关闭,随后用技能激活表达选择——教训:三选一弹窗在用户已有明确偏好时是噪音,下次列出来等口头拍板即可

**翻车与返工**
- 暂无(执行刚启动)

### 执行期事件:基线口径纠偏(巡检 1)

(执行期,用户无新输入)

- master 开跑前先实测基线,发现**并非我简报里的 209 passed,实为 243 passed + 1 failed**:`tests/test_eval_metrics.py::test_load_cases_derives_relevant_set` 红。209 是 ch03 早期口径,README 已写 244(commit `77e7e84`),我的执行纪律沿用了旧数——master 以实测为准,处理正确
- 该红测试疑似 ch03 收尾遗留(cd8fbf7 修过 `_load_cases` 派生 bug,测试名正相关),master 正在排查归属;结论待下次巡检补记
- 环境确认:Docker MySQL 在线(wayhelp-mysql healthy),T1 已派给 slave

### T1 基建:done(巡检 2,commit `e3ed348`)

(执行期,用户无新输入)

- 完成:httpx 提主依赖;Settings 13 个 ch04 字段 + has_rerank_key(embedding key 回退);`db/init/04-ddl.sql` 与 `sql/ch04-ddl.sql` 逐字节一致(cmp);dbfixtures 两新表先子后父;`check_ch04_tables` 缺表 RuntimeError
- 验证:master 复核真跑 `pytest tests/test_config.py tests/test_db_ch04.py tests/test_ddl_sync.py` → 22 passed;DDL 对 cmp 一致;T2 已派发
- 基线红归属查清(我抽查):`test_load_cases_derives_relevant_set` 会切真实 `knowledge_docs/*.md`,用户新四份 md 缺 frontmatter → `ChunkingError`(chunking.py:38);正是计划 T10 语料补录要修的缺口,提前在基线冒头,非外部回归,T10 后自愈——期间全量测试会保持 1 红,属预期
- 翻车与返工:无(master 一遍过)

### 执行期事件:T2 验证期全量 pytest 挂起(巡检 4,排查中)

(执行期,用户无新输入)

- 现象:master 跑 T2 验收的全量 pytest 挂死,faulthandler 240s 超时转储显示线程栈卡在 grpc _server 一带,361s 才退出;slave 侧同现象
- 初步方向:疑似 Milvus Lite/grpc 客户端未释放或 DB fixture 等待(已知脆点:Lite 客户端重开 released、env 污染均与 pymilvus import 副作用相关);master 正在读转储日志定位,T2 未提交
- 处置:不干预 master 排查;若下次巡检仍挂起且无结论,考虑提示 master 用 `pytest -p no:cacheprovider --forked` 或按模块分批跑缩小范围
