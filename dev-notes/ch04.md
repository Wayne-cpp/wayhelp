# Ch04 开发过程记录 —— RAG 进阶:混合检索 + 重排 + 生成质量控制 + 评估体系

## 阶段 1:Brainstorm 定稿(2026-09-15)

**用户关键原话**
- 「我要用 Superpowers 模式把客服系统的检索质量做上一个台阶:混合检索加重排,再配一套评估体系。」
- 「关键词召回这条路也落在 Milvus 上,用它 2.5 版起原生的 BM25 全文检索,text 字段挂 BM25 函数,中文分词用内置 chinese analyzer」「重排模型 bge-reranker-v2-m3」——技术选型定死
- 「涉及具体库、框架、API 的用法,一律先用 Context7 MCP 查最新官方文档和接口定义再动手,别凭记忆写」
- 「实现中发现矛盾或走不通,停下来问我,不要自行换方案」
- 「前端部分是例外,用 Vibe Coding 方式直接做」
- 「产出不是可单测代码的任务(纯 Prompt、数据类),把 TDD 那步换成拿标注样例或评估集跑一遍验证」
- 「[sql/ch04-ddl.sql] 这是建表语句」——low_confidence_questions + faith_cases 两张表 DDL 用户手写提供(沿用惯例)
- 「[retrieval_compare.txt] 这个是评估集」——300 条 CSV 评估集用户提供(A/B/C/E 各 60 + D_absent 拒答 60)
- 「我已经提供了,在 knowledge_docs 下,其他旧的都删除了」——新四份 md(喵喵优选宠物用品域)用户自备,旧三份已删

**澄清问答(AskUserQuestion 九轮)**
- Rerank 接入:用户选「硅基流动远程 API」——否掉本地 FlagEmbedding(~2GB 权重,与 ch03 否本地模型裁决一致)和独立推理服务
- 部署与迁移:用户三连反问(「是变 Standalone 吗」→「还是没看懂」→「为什么要动重建路径,不是用 milvus standalone 吗」→「重建后还需要用到 mysql 吗」)——暴露用户以为 BM25 必须上 Milvus Standalone;我用锁定版 milvus-lite 3.2.1 README 实测事实(纯 Python 重写代已支持 BM25/sparse/hybrid/RRF/Jieba)解释「不用换部署,重建的是集合 schema」+「MySQL 账本/Milvus 货架」分工,用户最终选「Lite 同名重建」
- 评估集组织:用户答「我会提供评估集,你告诉我路径」——我出格式与路径,用户随后直接给了 CSV 成品
- 元数据过滤:用户选「query_faq 加可选 category 参数」——否掉 query 理解模块统一产出、前端选择器
- 入池范围:用户选「两种拒答都入池」——**推翻其自写 DDL 注释**「本章只靠 useful 自评入池」,DDL 注释待修订(结构不动)
- 台账界面:用户选「本章只落表,处置用 SQL」——否掉 /kb 处置界面(DDL 注释里「人工点按钮改」留后续章节)
- 改写模型:用户选「复用主对话模型」——否掉独立模型配置
- 集成方案:用户选「retriever 内部升级」——否掉 HybridRetriever 并存、LangChain 全家桶
- 文档 frontmatter:用户选「我给四份文档补 frontmatter」——否掉放宽 chunking 契约
- 旧块清理:用户选「重建作业=全量重置」——挖掘块一并清(旧 FAQ 语境挖的,留着与新库矛盾)

**我的关键产出**
- 路径分类:architectural(检索/重排/Query 理解/生成控制/评估/前端六块 + 两张新表 + 评估基建)
- 动手前事实核查:Context7 核 pymilvus Function/BM25/add_function/hybrid_search/AnnSearchRequest/RRFRanker 签名;milvus-lite README 核实 Lite 3.x 支持 BM25(反驳了 2024-12 官方博客「Lite 不支持全文检索」的过时结论,系版本代际差异);**抓到用户选型与 Lite 的口径差**:「内置 chinese analyzer」在 Lite 上的等价物是 Jieba 分词器,参数形态列入脆点 C1
- 用户评估集核对:CSV 引号格式、五桶、`|`/`+` 期望章节规则、章节名与新文档标题对得上(如「MH-LP100」);D_absent 桶把拒答率纳入考核;发现新文档缺 ch03 必填 frontmatter、MySQL 存旧库脏块两个实施前置问题,均已裁决
- spec:`docs/superpowers/specs/2026-09-15-hybrid-retrieval-ch04-design.md`(14 节,commit `64ab607`),含证据组装「首尾排位」[1,3,5,7,9,10,8,6,4,2]、拒答双闸门与 source 判定矩阵、四策略评估指标(Recall@5/10 + MRR@10 + Faithfulness + 拒答正确率/误拒率)、faith_cases upsert 复发语义、Context7 必查清单与脆点 C1-C4
- 设计分三段呈现(§1-2 → §3-4 → §5-6),三次「ok」通过;git 基线:spec + 评估集 `64ab607`,知识文档换血 + ch04 DDL 草稿 `81e66b3`

**用户拒绝/纠偏**
- 本地 rerank 推理、Standalone 切换、HybridRetriever 并存、独立改写模型、台账界面、旧文档并存——均被否(见上)
- 用户自备的 DDL 注释(「只靠 useful 自评入池」)被用户本人口头推翻,以「两种都入池」为准

**翻车与返工**
- 暂无代码翻车;沟通层面:我对「重建」的初次解释用了太多术语(集合/schema/expr),用户连续三问才对齐——教训:对非专职 DB 用户,先打「表格结构/货架」比方再上术语

## 阶段 2:Spec 复审修订(2026-09-15)

**用户关键原话**
- 「spec我已经使用gpt6 review并修改了」——spec 经 GPT-6 复审修订(141 增 58 删,替我决定稿),沿用 ch03 惯例我逐项核真后并入(commit `7e2fd9f`)

**复审主要修订(GPT-6 稿并入,逐项核真)**
- 纠偏我原稿的「category 过滤」:现有 chunk.category 实为章节分类(如「商品规格手册」)而非商品品类;改为新增 `scope` 列(faq/policy/product_spec/after_sales_manual/qa_mined/manual 六枚举,content_type+source_doc 单一纯函数派生),过滤强制 `expr_params` 参数化防注入——**属实**,chunking.py 的 category 确为标题栈派生
- 语料缺口实测复现:补 frontmatter 后切出 **43 块(与修订稿完全一致)**;正例 GT 未覆盖 case 我算出 **24 条**(修订稿称「至少 17」,方向正确且保守),缺积分/可开票类型/开票时效/猫窝机洗/运费与包邮等章节——§11 语料补齐为真需求,「正文一字不动」约束作废
- D3 冲突属实:D3「能不能开纸质发票邮寄给我」(should_refuse=是)与 A30/E46 期望的企业纸质专票知识在语料补齐后必冲突;已按批准把 D3 改为「发票能不能用外币金额开具」
- 拒答升级确定性:检索硬闸门命中时**不发起第二次模型调用**,ChatService 直接输出固定拒答话术;生成侧自评改「去空白后与 REFUSAL_ANSWER 常量完全相等」才算,弃用我的宽松前缀匹配
- 入池原子化:commit_turn 扩展为 messages + low_confidence 同事务提交;系统故障(重建中/未配置/超时)明确**不得**伪装成 low confidence 入池
- 引用一致性:TurnToolset/RetrievalTrace 显式接口;唯一证据列表三处(tool 结果/citations SSE/envelope v2 metadata)逐项逐字段相等;每轮强制最多一次 query_faq;不推空 citations 帧
- 评估升级:固定奇偶分片(每桶奇 30 calibration/偶 30 test);新增 SectionRecall@K(AND 组覆盖率)/CompleteHit@K;四策略各自独立校准阈值(Pareto 候选 + 用户确认 D 桶误通过率约束,并列取误拒率低、阈值小);judge 结构化输出,失败记 judge_error 脚本非零退出;faith_cases 写主业务库跨轮累计;D3 修正纳入契约
- 工程硬约束:query_faq 独立 20s 总预算(monotonic deadline 全程共享,executor 零整链重试);rerank 内部 0.5s/1s 退避最多 3 次请求、耗尽降级 hybrid 并自动换用 HYBRID_MIN_SCORE;KnowledgeState 三态(ready/rebuilding/rebuild_required)+ 重建只读预检 + 维护状态;启动时只读校验两张 ch04 表存在

**我的关键产出(核真)**
- 引用点全部在位:commit_turn(sessions.py:20)、_tool_calls_legal(chat_service.py:284)、store_db.py/tool_envelope.py/schemas.py、job_busy(kb_admin.py:55);KnowledgeState 为新组件待建
- 奇偶分片数学自洽:五桶各 60 条,奇偶各 30;E27 的「猫窝」组也在未覆盖清单内,将由猫窝机洗新章节覆盖
- 三处动用户拍板项/用户文件,已停下问并获批准:scope 替代 category(接受)、D3 文案替换(接受)、语料由我补写 + validator 验收(接受)

**用户拒绝/纠偏**
- 我原稿「query_faq 出参带 section_path」被 GPT-6 改为「唯一 Evidence 列表三处相等、section_path 进 Evidence 但不单独投影」——采纳
- 我原稿「前缀匹配识别拒答」被收紧为精确相等——采纳
- 我原稿「rerank 走 executor RETRYABLE」被改为 reranker 内部重试 + 结构化降级,不进整链重试——采纳(避免叠乘)

**翻车与返工**
- 暂无;修订稿暴露我原稿两个语义糊涂处(category≠品类、拒答识别过宽),属计划前拦截

## 阶段 3:计划定稿(2026-09-15)

**用户关键原话**
- (spec 复审后无新指示,直接进 writing-plans)

**我的关键产出**
- 计划前脆点实测(替代纸面假设,全部锁定版本下亲跑):
  - C1 `analyzer_params={"tokenizer":"jieba"}` 建集合成;**JiebaAnalyzer 需 jieba 包** → 补依赖 jieba 0.42.1(uv add,推翻 spec「不新增第三方依赖」一句,已记 Global Constraints)
  - C2 BM25 查询 `data=[文本]` 直传命中型号文本 ✓;hybrid_search + RRFRanker(k=60) ✓,**RRF 分数量级 ~0.03**(阈值必须校准,严禁拍脑袋默认)
  - **翻车点实录**:`uv run` 自动加载 .env 使 pymilvus import 即炸 Illegal uri(项目 `_load_milvus_client` 隔离器就是为此存在,smoke 复用之即过);milvus-lite **不支持 expr_params/$占位符**(实测报错「only '$meta' is supported」)→ scope 过滤改枚举白名单+字面插值等效防注入,记为 spec §4.1 措辞的实测偏差;**run_analyzer UNIMPLEMENTED** → 改用 insert+search smoke 验证分词;重开客户端集合 released(既有 _ensure_loaded 坑,再次坐实)
  - 硅基流动 /v1/rerank 官方文档核签名:POST {model,query,documents,top_n} → results[{index,relevance_score}],用 index 回映射,不需 return_documents
  - httpx 在依赖树但位于 dev 组 → 计划 T1 提为主依赖
- 实施计划 `docs/superpowers/plans/2026-09-15-hybrid-retrieval-ch04.md`(2763 行,14 任务,commit `e027203`):T1 基建/T2 ORM+同事务入池/T3 五列 schema/T4 Query 理解/T5 reranker/T6 retriever 管道/T7 工具层+envelope v2/T8 提示词/T9 ChatService 双闸门/T10 语料补录+validator/T11 重建作业+装配/T12 评估脚本/T13 前端 vibe/T14 README+验收
- 计划自审抓到并修掉 5 处:T6 证据组装占位残留行、未用变量 empty、hybrid leg_counts 语义造假(改 fused 记法)、T9 second_skipped 与 else 互相覆盖(重排为 calls 内分流)、T2 source 类型标注与实现不一致、faith 测试同义反复断言——**ch03「计划内嵌代码编译级自查」教训首次主动执行**

**用户拒绝/纠偏**
- 暂无(待评审)

**翻车与返工**
- 计划前 smoke 翻车 2 次(env 污染、released 集合),均为已知坑的再次验证,未流入计划

## 阶段 4:执行启动——herdr-duo 接管(2026-09-15)

**用户关键原话**
- 「[herdr-duo] 根据plan实现计划」——执行方式三选一弹窗被关闭后,用户直接激活 herdr-duo 技能拍板:双代理(claude 执行 / kimi 复核)跑计划

**我的关键产出**
- herdr session `dev-wayhelp` 搭起:master(kimi,w1:p1)+ slave(claude,w1:p2),均 idle,工作目录即仓库根
- 已把计划 `docs/superpowers/plans/2026-09-15-hybrid-retrieval-ch04.md` 交给 master,附带六条执行纪律:定死选型不自换、库 API 先查官方文档、每任务真跑验证命令(基线 209 passed)、T13 走 vibe 例外、每任务验收后 Conventional Commits 提交、spec 优先;要求最终总结逐任务报完成情况/验证结果/翻车返工
- 巡检 cron 已挂(id `01M2JD6D4FC5AJMQPEZQVM57JT`,每小时 9/29/49 分):按 git log + master 输出逐任务追记本文件,收尾时删除

**用户拒绝/纠偏**
- 执行方式弹窗(Subagent-Driven 推荐/herdr-duo/Inline)用户未答直接关闭,随后用技能激活表达选择——教训:三选一弹窗在用户已有明确偏好时是噪音,下次列出来等口头拍板即可

**翻车与返工**
- 暂无(执行刚启动)

### 执行期事件:基线口径纠偏(巡检 1)

(执行期,用户无新输入)

- master 开跑前先实测基线,发现**并非我简报里的 209 passed,实为 243 passed + 1 failed**:`tests/test_eval_metrics.py::test_load_cases_derives_relevant_set` 红。209 是 ch03 早期口径,README 已写 244(commit `77e7e84`),我的执行纪律沿用了旧数——master 以实测为准,处理正确
- 该红测试疑似 ch03 收尾遗留(cd8fbf7 修过 `_load_cases` 派生 bug,测试名正相关),master 正在排查归属;结论待下次巡检补记
- 环境确认:Docker MySQL 在线(wayhelp-mysql healthy),T1 已派给 slave

### T1 基建:done(巡检 2,commit `e3ed348`)

(执行期,用户无新输入)

- 完成:httpx 提主依赖;Settings 13 个 ch04 字段 + has_rerank_key(embedding key 回退);`db/init/04-ddl.sql` 与 `sql/ch04-ddl.sql` 逐字节一致(cmp);dbfixtures 两新表先子后父;`check_ch04_tables` 缺表 RuntimeError
- 验证:master 复核真跑 `pytest tests/test_config.py tests/test_db_ch04.py tests/test_ddl_sync.py` → 22 passed;DDL 对 cmp 一致;T2 已派发
- 基线红归属查清(我抽查):`test_load_cases_derives_relevant_set` 会切真实 `knowledge_docs/*.md`,用户新四份 md 缺 frontmatter → `ChunkingError`(chunking.py:38);正是计划 T10 语料补录要修的缺口,提前在基线冒头,非外部回归,T10 后自愈——期间全量测试会保持 1 红,属预期
- 翻车与返工:无(master 一遍过)

### 执行期事件:T2 验证期全量 pytest 挂起(巡检 4,已结案)

(执行期,用户无新输入)

- 现象:master 跑 T2 验收的全量 pytest 挂死,faulthandler 240s 超时转储显示线程栈卡在 grpc _server 一带,361s 才退出;slave 侧同现象
- ~~初步方向:疑似 Milvus Lite/grpc 客户端未释放或 DB fixture 等待~~ **误向,已结案**:转储里的 grpc 栈只是 139 个 Milvus Lite 后台线程的旁观者;真凶是 T2 自己——`tests/test_spec11_safety.py:136` 的 `slow_commit(session_id, messages)` 两参 monkeypatch,遇 `commit_turn` 新的三参调用 `self._commit_sync(session_id, messages, low_confidence)`,TypeError 在 to_thread 工作线程内爆发,`entered.set()` 永不执行,主协程卡死在 `await asyncio.to_thread(entered.wait)`,无超时、无输出、无 summary
- 定性:计划「既有调用方全部兼容」只核对了 `commit_turn` 公开调用方,漏了测试对内部方法 `_commit_sync` 的直接 patch——T2 引入的真实回归,非环境脆点
- 铁证:git stash 改动后该测试 0.61s 通过,恢复改动即挂起(45s timeout 击杀);修复后全量 54.02s 跑完
- 修复:经用户批准(超出 T2 文件清单故请示),slow_commit 改 `(session_id, messages, low_confidence=None)` 并透传;生产代码不动
- 经验:全量「无输出挂死+线程转储指向第三方库」时,先找变更集里动过签名的被 patch 内部方法,再怀疑基础设施;monkeypatch 包装层必须与被包方法签名同步演进

### T2 ORM 两表 + commit_turn 同事务入池:done(commit `04971f9`)

(执行期,用户无新输入)

- 完成:`models.LowConfidenceQuestion/FaithCase` 逐列对齐 `sql/ch04-ddl.sql`;`sessions.LowConfidenceRecord` + `SessionStore.commit_turn(low_confidence=None)`(既有调用方零改动);`InMemorySessionStore.low_confidence` 校验全过后才入池(与 DB 同成同败);`store_db._commit_sync` 同事务追加写入池行,任一失败整体回滚
- TDD 全程红→绿:test_models 2 追加(ImportError 红→绿)、test_store_db 1 追加、test_sessions 2 追加(入池/校验不过不入池);写 ORM 前经 Context7 核对 SQLAlchemy 2.0 官方文档(字符串 Enum+name、Mapped[Optional] 定 nullable、server_default)——与计划写法一致
- 验收:三文件 24 passed;全量 `1 failed, 253 passed in 54.02s`(253=基线 248+新增 5,唯一红仍是 T10 待修的 `test_load_cases_derives_relevant_set`,未动)
- 翻车与返工:见上节挂起事件——一轮全量因此报废 36 分钟,根因与修复同节记;无其他返工

### T3 milvus_store 五列 schema + 双路检索 + hybrid + scope 过滤:done

(执行期,用户无新输入;单任务直派,commit 见本节末)

- 完成:`scope.py`(SCOPES 六枚举 + derive_scope 单一派生);`milvus_store` 五列 schema(id/vector/text/sparse/scope,text 列 `enable_analyzer + jieba`,sparse 由 Milvus 原生 `Function(FunctionType.BM25)` 生成,双索引 vector=AUTOINDEX/COSINE、sparse=SPARSE_INVERTED_INDEX/BM25);upsert 改四元组 (id, vector, text, scope);`search_dense`/`search_bm25`(anns_field=sparse)/`hybrid`(`AnnSearchRequest` ×2 + `RRFRanker(k=60)`);`_scope_filter` SCOPES 白名单校验后字面插值(Lite 不支持 expr_params/$var,实测脆点 C2 既载);`drop_collection`/`recreate`;旧 `search()` 已删,调用方 T6 切换
- 写码前 API 核对:经 Context7(`/milvus-io/pymilvus`)核对 create_schema/add_field(analyzer_params)/Function/FunctionType.BM25/AnnSearchRequest(有 expr 形参,传 None 合法)/RRFRanker/MilvusClient.hybrid_search(collection_name, reqs, ranker, limit)——与计划写法一致,零偏差
- **smoke 核销结论(计划内嵌核销步,已先跑)**:Lite 的 `describe_collection` **确实回传 `functions` 键**,形态 `list[dict]`(name='bm25_fn'/type=FunctionType.BM25/input_field_names=['text']/output_field_names=['sparse']),`_verify_contract` 的函数断言按计划草稿定稿,**无需降级**为「字段齐+dim 对」;另证:空集合三路检索不炸;upsert 后 BM25 真命中(型号词 MH-LP100→id=1);hybrid RRF 分数 0.0328,量级 ~0.03 与 C2 记载一致
- 偏差(计划漏点,已处理):计划对 tests/test_milvus_store.py 只写「追加」,但既有旧签名测试(upsert 二元组/search() 的 6 处,含崩溃恢复子进程脚本内嵌调用)必须同步迁移到四元组 + search_dense,否则验收命令 1 无法全绿;已迁移,断言语义不变(仅补 text/scope 列)
- 验收:`uv run pytest tests/test_scope.py tests/test_milvus_store.py -v` → 17 passed;全量 `17 failed, 241 passed in 76.17s`。红清单归属:① `test_eval_metrics::test_load_cases_derives_relevant_set` 1 个=既有基线红(T10 自愈,未动);② 签名切换 16 个=`test_ingest` 7(ingest.py:58 二元组 upsert)+ `test_mining` 5(mining 复用 `ingest.vectorize_pending` 传导,日志铁证「ValueError: not enough values to unpack (expected 4, got 2)」)+ `test_retriever` 4(retriever.py:123 已删的 store.search + 测试内二元组 upsert);③ `test_kb_api` 不红——其 store 为测试本地 FakeKbStore 桩,不碰真 store。数字对账:基线 253 passed + 新增 4 − 转红 16 = 241 ✓,无计划外红。T3-T6 commit 链期间留红仓属预期,T6 末尾回绿
- 观察项(非失败):全量 stderr 出现 grpc `AllocTimestamp NotImplementedError` 转储——milvus-lite 后台线程旁观者噪音(与 T2 挂起事件同类旁观者,非真凶),不影响测试通过,不处理

### T4 Query 理解(改写+同义词,降级直查):done

(执行期,用户无新输入;commit 见本节末)

- 完成:`app/prompts/query_understanding.py`(QUERY_UNDERSTANDING_PROMPT,单行 JSON 契约);`app/knowledge/query_understanding.py`:`QueryPlan` frozen dataclass(standard_query/synonyms/rewrite_model/degraded/note)、`passthrough_plan`、`parse_plan`(正则提取 JSON,非 JSON/空改写降级,同义词过滤截 5)、`plan_query(model, query, *, enabled, timeout_seconds, model_name=None)`——disabled/无模型/异常/解析失败四路全部降级原问法直查,不阻断检索
- TDD:红(ModuleNotFoundError)→ 绿,`tests/test_query_understanding.py` 5 例(正常解析/非 JSON/模型异常/disabled/空改写)全过
- **偏差(计划骨架真 bug,已修)**:骨架的 `QUERY_UNDERSTANDING_PROMPT.format(query=query)` 与 prompt 内字面 JSON 花括号冲突——`str.format` 把 `{"standard_query"...}` 当替换字段抛 `KeyError`,被 except 吞掉后全部降级,`test_parse_ok` 红(铁证:日志 `query understanding failed: KeyError`);改为 `QUERY_UNDERSTANDING_PROMPT.replace("{query}", query)`(带注释),prompt 文件保持计划原文不动
- 验收:`uv run pytest tests/test_query_understanding.py -v` → 5 passed;全量 `timeout 600 uv run pytest -q -o faulthandler_timeout=240` → `17 failed, 246 passed in 76.46s`。红清单与 T3 末尾完全一致(签名切换 16 + 既有基线红 1),无新增红;对账:241+新增 5=246 ✓。T6 回绿预期不变
- 翻车与返工:仅上述 format 花括号一轮红修;无其他

### T5 reranker(硅基流动 /v1/rerank,内部重试 + 结构化降级):done

(执行期,用户无新输入;commit 见本节末)

- 完成:`app/knowledge/reranker.py` —— `RerankOutcome` frozen dataclass(ok/ranking/note,ranking=(候选下标, relevance_score) 按分数降序)、`SiliconFlowReranker(settings, client=None)` + `.rerank(query, documents, top_n, deadline=None)`;无 key → `rerank_key_missing`、空 documents → `(True, [], None)` 直通;连接/超时/408/409/429/5xx 退避重试(0.5s/1s,max_retries=2 共 3 次请求),401/403/其他 4xx 零重试;deadline 预算耗尽短路 `budget_exhausted`,单请求 timeout=min(配置超时, 剩余预算, 下限 0.1s);任何失败耗尽返回 `RerankOutcome(False, [], note)`,绝不抛给 executor
- 写码前 API 核对:经 Context7(`/encode/httpx`)核对 Client.post 每请求 timeout 浮点参数、异常层级(TransportError ⊃ TimeoutException/NetworkError ⊃ ConnectError,计划捕获元组覆盖连接+超时全部)、MockTransport(handler) + `httpx.Response(json=...)` 官方测试用法——与计划写法一致,零偏差
- TDD:红(ModuleNotFoundError)→ 绿,`tests/test_reranker.py` 5 例(解析排序/500 重试耗尽降级共 3 次/401 零重试/ConnectError 重试耗尽/deadline 短路)全过
- 验收:`uv run pytest tests/test_reranker.py -v` → 5 passed in 1.54s(含 test_retry_exhausted_degrades 真睡 0.5s+1s 退避,计划如此设计);全量 `timeout 600 uv run pytest -q -o faulthandler_timeout=240` → `17 failed, 251 passed in 77.66s`。红清单与 T4 末尾完全一致(eval_metrics 基线 1 + 签名切换 16),无新增红;对账:246+新增 5=251 ✓;grpc AllocTimestamp 噪音仍为 milvus-lite 旁观者(T3 已记)。T6 回绿预期不变
- 翻车与返工:无,一轮红→绿

### T6 retriever 四级管道 + 证据组装 + ingest 新 upsert:done

(执行期,用户无新输入;commit 见本节末)

- 完成:`retriever.py` 重构——`KnowledgeHit` 补 section_path;`RetrievalResult`(hits/requested/effective/confidence_score/confidence_threshold/low_confidence/note/query_plan/leg_counts);`Evidence` + `assemble_evidence`(截 max_items → `display_permutation` 首尾排位 → 预算累加整条弃 → 裁剪后才编 ref_no)+ `display_permutation`;`KnowledgeRetriever(settings, embed, store, session_factory, model, reranker, state)` 四级管道:维护态(`_state_note`:rebuilding/rebuild_required)→ enabled/file_exists/has_collection 前置 → Query 理解(model 在位才真改写)→ 双路召回(dense/bm25/hybrid 按 `knowledge_strategy` 分派,`retrieval_candidate_k` 腿深,hybrid 腿 `leg_counts={dense:k, bm25:k, fused:len}` 腿级命中数不可得)→ rerank(hits 非空才调,`rerank_top_n` 截断);阈值不做命中过滤,只驱动 low_confidence;`_THRESHOLD_ATTR` 分策略取值,rerank 降级 effective=hybrid 时阈值换 `hybrid_min_score`;`_as_retryable` 语义原样保留;`probe` 改 dict 旁路。`ingest.py` upsert 改四元组 (id, vector, text, scope),scope 经 `derive_scope(content_type, source_doc)`。mining.py/kb_admin 手工录入经 `vectorize_pending` 传导,grep 确认无直调 upsert,零改动;`kb_admin.search_probe` 适配新 probe dict
- TDD:红(evidence ImportError + retriever 10 例 TypeError)→ 绿;`tests/test_evidence.py` 5 例(排位 n10/n6/n1、预算整条弃、budget=1 空)全过;`tests/test_retriever.py` 重写(管道 5 例 + 前置状态 3 例 + query_faq 契约 2 例 + `_as_retryable`/executor 3 例保留)全过;`test_kb_api` FakeKbStore 补 `search_dense/search_bm25/hybrid`(朴素 RRF 同构)桩,upsert 兼容 2/4 元组并存 text 供 bm25 桩用
- **偏差 1(计划测试不可通过,已改编)**:`test_zero_hits_low_confidence` 原文不可红——①计划注释「登录→不相似向量」与既有种子矛盾(种子 e1 块就是登录块,cos=1.0);②FakeReranker 恒给 top1=1.0 且 `rerank_min_score` 默认 0.0,低置信永不可触发;③新管道不按阈值过滤 hits,正交向量也回 0 分候选(旧测试注释「e2 方向被阈值滤掉」已证 Milvus 会回 0 分),零命中分支不可达。改编:查询换「发票怎么开」(FakeEmbeddings 其余→e3,真不相似)+ `strategy="dense"` + `min_score=0.5`,保住「定向不相似 → 低置信」原意
- **偏差 2(no_model note 污染)**:probe→search 无 query_plan 且无 model 时 `plan_query(None)`→note="no_model",会把既有 /kb 自测测试 `note is None` 打红。改:`self._model is None` 时静默 `passthrough_plan(query)`(配置态不当降级 note);model 在位仍走真实改写(含 rewrite_disabled/rewrite_error note),T7 生产路径 model 已装配不受影响
- **过渡决策(kb_admin)**:`search_probe` 传 `strategy="dense"`——旧 probe 本就是 dense 自测,且此处未装配 reranker,放行默认 hybrid_rerank 会恒降级 `reranker_not_configured`;T11 接 strategy/scope 透传与 reranker 装配后自然替换
- **过渡决策(business.py 保活)**:business.py(T7 文件,本任务禁触)仍是 `(hits, note)` 二元组 + 阈值过滤旧契约,test_retriever 的两个 query_faq 契约测试经 `_LegacyFaqRetriever` 适配器投影新 `RetrievalResult` 保活(含按 `confidence_threshold` 过滤复刻旧语义),T7 工具层改造时随测试一并删除。已知代价:生产 query_faq 在 T6→T7 间对新 API 类型不匹配(T7 紧随落地,同计划内闭环)
- 验收:命令 1 `tests/test_evidence.py test_retriever.py test_ingest.py test_mining.py test_milvus_store.py -v` → **57 passed**;命令 2 全量 `timeout 600 uv run pytest -q -o faulthandler_timeout=240` → **`1 failed, 274 passed in 79.99s`**。**T3 红窗关闭确认**:16 个签名切换红(ingest 7 + mining 5 + retriever 4)全消,仅剩 1 个既有基线红 `test_eval_metrics::test_load_cases_derives_relevant_set`(T10 修),无新增红;对账:T5 末 251 passed/17 failed(共 268 例)→ 本任务末 274/1(共 275 例);passed 增量 23 = 红窗 16 转绿 + 净增 7(evidence 新文件 5 例 + retriever 11→13 例重写净增 2,kb_api 例数不变仅补桩)✓;grpc AllocTimestamp 噪音仍为 milvus-lite 旁观者(T3 已记),不处理
- 翻车与返工:偏差 1 的测试设计推演(Milvus 0 分候选回包)在写码前想清,未产生废轮;其余一轮红→绿

### T7 工具层(TurnToolset/RetrievalTrace/query_faq scope/executor 独立预算/envelope v2):done(commit `23ef08a`;原记 a36c5ac 为改写前哈希,巡检更正)

(执行期,用户无新输入)

- 完成:`tool_envelope.py`——`wrap(..., metadata=None)` metadata 非 None → v2(v1 输出逐字节不变,既有 6 例零改动全绿),`unwrap` 接受 v1/v2 其余版本仍抛 ValueError,`unwrap_metadata` v2 取 dict/v1 返 None;v2 超长只截 content 标 truncated,metadata 原样保留。`executor.py`——`tool_policies(name→(timeout, max_retries))` 命名单独生效,`_run_readonly` 改收局部 timeout/max_retries,write 路径与未命中名的默认策略不变。`business.py`——`RetrievalTrace`(可变,not_called 起步完成后只写一次)+ `TurnToolset(tools, retrieval_trace)` + `tools_by_name`;`build_tools(..., settings=None) -> TurnToolset`;query_faq 新出参 `evidence/effective_strategy/low_confidence/note` + 可选 `scope`(SCOPE_ENUM 六枚举 Literal,pydantic 层拒非法值,Context7 核 langchain 官方 Literal args 用法一致);REBUILDING/REBUILD_REQUIRED/UNCONFIGURED 三 note → tool_error 分支映射 kb_rebuilding/kb_rebuild_required/kb_unconfigured;evidence 经 `assemble_evidence`(max_items=rerank_top_n, budget=max_tool_result_chars, overhead=200)与 trace.evidence 同一份列表。`conftest.make_runtime` 的 toolset_factory 返回 TurnToolset;删除 T6 过渡 `_LegacyFaqRetriever` 及两个保活用例
- **偏差(计划缺口,已补桥接测试与实现)**:计划 Step 6 让 factory 返回 TurnToolset,但 T9 才改 ChatService 消费 `ts.tools`/`ts.retrieval_trace`——期间 ChatService 仍把 factory 结果直喂 `ToolRegistry(tools)`(迭代)与 `if tools`(真值判断),裸 dataclass 两者皆炸(TypeError/恒真),波及 make_runtime 全系(test_chat_api/test_chat_api_tools/test_kb_api/test_lifespan/chat_page/kb_page/extract_api)与 test_spec11_safety 的 build_tools 直用、生产 main.py factory 同理。修:TurnToolset 补 `__iter__/__len__/__getitem__` 列表协议(带 T9 桥接注释),新增 `test_turn_toolset_satisfies_legacy_list_contract` 一例 TDD 覆盖(ToolRegistry 直喂 + 空集为假)
- 既有坑复用:T6 记载的「call dict 缺 `type: "tool_call"` → langchain 把信封当 args,工具体不执行」在计划 Step 3 原文再次出现,两用例构造时直接补上,断言不变
- 验收:命令 1 `tests/test_tools.py tests/test_executor.py tests/test_tool_envelope.py -v` → **29 passed**;命令 2 全量 `timeout 600 uv run pytest -q -o faulthandler_timeout=240` → **`1 failed, 281 passed in 79.32s`**,唯一红仍是 `test_eval_metrics::test_load_cases_derives_relevant_set`(T10 修,未动),test_chat_service/test_orchestration 全绿(factory 契约桥接生效,无需返修);对账:T6 末 274 passed/1 failed(275 例)→ 本任务末 281/1(282 例),净增 7 = envelope 2 + executor 2 + tools 5 − retriever 删 2 ✓
- 翻车与返工:无,三段各自一轮红→绿;列表协议缺口在写码前推演发现(先 grep factory 消费方),未产生废轮

### T8 提示词(引用协议/拒答话术/负面知识/judge):done(commit `7ac8baf`,巡检补)

(执行期,用户无新输入)

- 完成:`app/prompts/service.py` 重写——新增 `REFUSAL_ANSWER` 固定拒答话术常量;`SERVICE_SYSTEM_PROMPT` 改 f-string 引用该常量(第 12 条逐字内嵌),结构升为三段:行为约束 9 条(旧 10 条重排:旧 #3 赔偿/时限承诺条款并入新 #13 负面知识禁令;旧 #10 工具纪律并入新 #9 并扩「query_faq 每轮最多调用一次,把完整问题一次传入」)+ 知识引用协议 3 条(#10 [ref_no] 角标格式与连标、#11 无证据禁答/evidence 空或 low_confidence 禁答、#12 证据不足只回固定话术)+ 负面知识禁令 2 条(#13 不承诺个案到账/发货/送达日期与赔偿金额,通用政策时效须表述为「政策规定/通常/应在……内」;#14 禁用知识库未载明的承诺类表述)。「演示数据」「不得承诺」等既有契约字样在新文案中保留。`app/prompts/faithfulness.py` 新建——`JUDGE_PROMPT` 忠实度裁判模板,JSON 输出示例 `{{}}` 双花括号转义预留 `.format(query=, answer=, evidence=)` 占位。`tests/test_prompts.py` 新建 2 例:REFUSAL_ANSWER 在 prompt 中逐字内嵌且恰好一次 + 四个契约字样([ref_no]/low_confidence/不承诺/query_faq 每轮最多调用一次)存在
- 既有断言核查(预期内文案更替排查):grep 全仓 `SERVICE_SYSTEM_PROMPT` 消费方——`app/main.py` 仅导入传递(接口不变);`tests/test_chat_api_tools.py:82-83` 断言「演示数据」「不得承诺」在新文案 #8/#9/#13 中均仍存在,两例全绿,**未改动任何既有断言**
- 验收:命令 1 `uv run pytest tests/test_prompts.py -v` → **2 passed in 0.01s**;命令 2 全量 `timeout 600 uv run pytest -q -o faulthandler_timeout=240` → **`1 failed, 283 passed in 79.40s`**,唯一红仍是 `test_eval_metrics::test_load_cases_derives_relevant_set`(T10 修,未动),无新增红;对账:T7 末 281 passed/1 failed(282 例)→ 本任务末 283/1(284 例),净增 2 = test_prompts 新文件 2 例 ✓;grpc AllocTimestamp 噪音仍为 milvus-lite 旁观者(T3 已记)
- 翻车与返工:无;Prompt 类任务按用户规矩以常量引用一致性断言替代完整 TDD(计划已写明),一次通过

### T9 ChatService(citations 帧 + 拒答双闸门 + 同事务入池)+ 路由:done(commit `8c59797`)

(执行期,用户无新输入)

- 完成:`chat_service.py`——新增 `CitationsEvent(citations: list[dict])` 并入 `ChatEvent` Union;toolset 解包改 `ts.tools`/`ts.retrieval_trace`(hasattr 分派,裸 list factory 旧装配仍兼容);`ToolExecutor` 配 `tool_policies={"query_faq": (knowledge_tool_timeout_seconds, 0)}`;`_tool_calls_legal` 加 query_faq 每轮最多一次;硬闸门:工具执行循环后 `trace.status=="low_confidence"` → 不发起第二次模型调用,直接 `DeltaEvent(REFUSAL_ANSWER)`(第二次调用整段进 else 分支,plan 自审预修正的「second_skipped 与 else 互相覆盖」结构落地);统一收尾:拒答入池判定(`low_confidence`+final strip==REFUSAL_ANSWER → `retrieval_low_conf`,reason 含 requested/effective/top1/threshold/note;`ok`+同条件 → `self_check`,reason 含 evidence_refs;`tool_error` 不入池,走第二次模型如实说明),`conversation_id` 仅 `session_id.isdecimal()` 才转 int 否则 None;query_faq 成功行落库 envelope v2 `metadata={"citations": trace.evidence, "retrieval": {五项指标}}`(与 query_faq 出参 evidence、CitationsEvent 三处同一份列表,test 逐字段核销);citations 帧仅在 `ok` 且 evidence 非空且非拒答时推,位于 commit 之后、DoneEvent 之前。`routers/chat.py`——`CitationsEvent` 分支 `{"type":"citations","citations":[...]}`(Context7 核 FastAPI SSE:本仓手写 `data:` 格式 StreamingResponse,不动协议)
- 测试:service 5 例(硬闸门跳二次调用+入池 reason 五键/conversation_id None、自评入池 self_check 且仍走第二次调用、citations 帧+三处同列表+顺序、tool_error 不入池双调用、query_faq 双调 invalid_tool_call)+ api_tools 2 例(SSE citations 帧顺序与拒答无 citations)+ api 1 例(帧 schema `set(cit)=={"type","citations"}` + 最后 delta 后/[DONE] 前)
- 偏差(计划骨架微调,3 处):①计划 `tools = ts.tools` 直取会让 test_orchestration 的裸 list factory 炸 AttributeError → 改 hasattr 分派(与 T7 桥接注释的「T9 只改消费方式」一致,列表协议按用户指示保留);②计划测试代码里 tool_call chunk 的 `args` 写成 dict,与 `merge_tool_call_chunks` 的字符串累加契约不符(T6/T7 已两次踩坑)→ 用 JSON 字符串;③计划骨架内联 `import json as _json` → 提为模块级 `import json`
- 验收:命令 1 `uv run pytest tests/test_chat_service.py tests/test_chat_api.py tests/test_chat_api_tools.py tests/test_orchestration.py -v` → **57 passed**;命令 2 全量 `timeout 600 uv run pytest -q -o faulthandler_timeout=240` → **`1 failed, 291 passed in 79.27s`**,唯一红仍是 `test_eval_metrics::test_load_cases_derives_relevant_set`(T10 修,未动);对账:T8 末 283 passed/1 failed(284 例)→ 本任务末 291/1(292 例),净增 8 = service 5 + api_tools 2 + api 1 ✓;starlette testclient anyio DeprecationWarning 为既有旁观者噪音
- 翻车与返工:无,8 例一轮红(RED 证据:硬闸门用例泄漏「不应被调用」、CitationsEvent ImportError)→ 绿

### T10 评估集 loader + 语料补录 + corpus validator:done(commit `11630ae`)

(执行期用户有一次决策输入:既有红测试修法)

- 完成:`app/knowledge/evalset.py`——`EvalCase`(frozen dataclass)/`parse_gt`(文法 + AND、| 与 / 等价 OR;空组/连续运算符 ValueError)/`covered_groups`(section_path 子串命中,一块可覆盖多组)/`load_compare_cases`(csv.DictReader 六列,奇数编号 calibration 偶数 test,60/桶与 30/30 分片及 D 桶拒答纯度加载期断言)/`BUCKETS`。`tests/test_evalset.py` 4 例 TDD(文法/非法语法/覆盖计数/300 条实数据结构),一轮红(ModuleNotFoundError)→绿。`evals/validate_corpus.py`——离线 chunk 四文档,正例双重校验(GT 组至少一别名命中 section_path + expect_points 去空格后为 questions+answer 拼接的子串),D 桶跳过;`evals/retrieval_compare.txt` 首次全量加载验证(300 条/5 桶×60/每桶 30-30,与 spec §4.5 一致)
- 语料补录:四文档顶部加 frontmatter(faq/policy/manual/manual,正文零改动);product-faq 追加六节(会员权益 / 会员等级、积分怎么攒、积分怎么用、可开票类型、开票时效、运费与包邮),product-specs 追加「猫窝是否可以机洗」;validator 首跑 30 条失败(A 桶 14/C 桶 3/E 桶 13)全数归于上述缺口,补录后一轮清零
- **偏差(4 处,均有实测依据)**:①validator 骨架 `Settings(_env_file=None)` 缺 openai_base_url 等 4 个必填字段直接 ValidationError → 按 conftest.make_settings 占位法补齐(保持离线);②计划 Step 5 文案两处不含字面要点——「单笔订单最多抵 30 元」不含 A26/E50 要点「单笔最多抵 30 元」、「不参与普通包邮」不含 E32 要点「不参与包邮」(validator 为去空格子串语义),补写时改为「单笔最多抵 30 元」「不参与包邮」,避免二轮返工;③计划列出的「会员运费权益」「会员专属售后权益」两节及 returns-policy/after-sales-manual 全部预缺口实测已覆盖(会员运费权益既有文案已含 E9/E16 全部要点),零改动;④既有红 `test_eval_metrics::test_load_cases_derives_relevant_set` **frontmatter 修不绿**——ch03 的 jsonl 标注引用已删除的旧文档名(商品FAQ.md 等 22 处)且 `len(corpus_keys)==14` 断言对 ch04 语料实测 43 块(补录后 50)不成立,经用户确认重写为 tmp_path 合成标注集(22 正/20 负含 p_youfei,monkeypatch CASES)钉同一回归目的(relevant 派生),不再依赖真实 knowledge_docs
- 验收:命令 1 `uv run python evals/validate_corpus.py` → **`[validate] 50 块;失败 0 条`,exit 0**;命令 2 `uv run pytest tests/test_evalset.py tests/test_chunking.py -v` → **28 passed**(evalset 4 + chunking 15,另附跑 test_eval_metrics 9 例全绿);命令 3 全量 `timeout 600 uv run pytest -q -o faulthandler_timeout=240` → **`296 passed, 1 warning in 79.24s`,0 失败**;对账:T9 末 291 passed/1 failed(292 例)→ 本任务末 296 passed(296 例),净增 4 = test_evalset 新文件 4 例 ✓,既有 1 红转绿 ✓;starlette anyio DeprecationWarning 为既有旁观者噪音(T8 已记)
- 翻车与返工:无;loader 一轮红→绿,validator 30 失败一轮清零(补写前先拉全 30 条原始 GT/要点逐字比对,计划文案两处子串不命中在写入前发现)

### T11 重建作业 + KnowledgeState + main 装配 + probe 升级:done(commit `7ce617c`)

(执行期,用户无新输入)

- 完成:`app/knowledge/state.py`——`KnowledgeState`(ready/rebuilding/rebuild_required)+ `KnowledgeStateHolder`(threading.Lock get/set,进程内唯一持有者)。`milvus_store.py`——`contract_error()` 只读契约探针:集合不存在返回 None(未建库走 NOTE_NOT_BUILT 语义,不创建集合),存在但契约不符返回 `_verify_contract` 文案。`kb_admin.py`——`rebuild_index`(spec §6 全量重置):无 key 503 → `_job_lock` 互斥 → `_preflight` 只读预检(文档可切/评估集 GT 全覆盖[T10 evalset 复用]/tempfile 新 schema 干跑 + BM25 smoke,失败抛 `RebuildPreflightError` 409,不动旧库)→ state=rebuilding → drop_collection + `_clear_knowledge_tables`(知识三表,先 UPDATE 解 prev/next 自引用链再多行 delete)→ ensure_collection → run_ingest 重灌 → BM25 型号 smoke 终验,任一失败 state=rebuild_required 并抛 `RebuildFailedError` 500,成功 state=ready + check_consistency,message 含块数/双写一致/smoke 命中;`get_state` 加 state 参暴露 `knowledge_state`;`search_probe` 透传 strategy/scope 并复用装配 retriever。`routers/kb.py`——`POST /kb/api/rebuild`;/state 注入 holder;/search 透传 + 传 `app.state.retriever`。`schemas.py`——`KbSearchRequest` 加 strategy(四策略)/scope(六枚举)Literal,缺省 None 服务端自选。`main.py`——`_build_production_runtime(settings, model)` 注入模型(改写腿用);启动 `check_ch04_tables`(缺表 RuntimeError 带升级命令,直通不包一层);Milvus 文件在场时旧 schema 探针 → 初值 holder 置 rebuild_required(探针异常同样置);`SiliconFlowReranker` 按 has_rerank_key 装配;retriever 带 model/reranker/state;toolset_factory 带 settings;`app.title=wayhelp-ch04`;AppRuntime 加 knowledge_state 字段,装配对缺省 runtime 补默认 holder;新增 `app.state.retriever` 暴露。conftest.make_runtime 同步带 holder(FastAPI/SQLAlchemy 写前经 Context7 核对:Query.update/delete `synchronize_session=False` 批量语义、自定义异常处理器注册,与仓库既有用法一致)
- 测试:kb_api 4 例(rebuild ok:旧块全清/两库一致/message 前缀/state 回 ready;preflight 切块炸:409 rebuild_preflight + store.dropped False + MySQL 行不动 + state 仍 ready;终验 smoke 恒空:500 rebuild_failed + dropped True + state=rebuild_required,恢复 search_bm25 后重跑仍先预检可回 ready;search 透传 bm25+faq:requested/effective/leg_counts/hits)+ schemas 6 例(缺省 None/合法透传/非法枚举×4)+ main_boot 3 例(title/holder 暴露/缺省 runtime 补默认 holder);FakeKbStore 补 `drop_collection`(清 vectors/texts,防旧 id 残留致 _verify 假红)/`recreate`/`contract_error` 桩
- 偏差(6 处,均有依据):①`_preflight` 签名去掉计划中未用的 session_factory/store 参,并把「可切检查」「GT 覆盖」两遍切块合并为一遍(攒 section_paths 复用),语义等价;②search_probe 加可选 retriever 参——生产走装配 retriever(reranker/model/state 齐备,缺省策略按配置 hybrid_rerank 真重排),旁路回退自建时缺省仍钉 dense(无 reranker 放行默认 hybrid_rerank 恒降级,既有 test_search_probe_passed_flags 的 note None 也依赖此);③`app.state.knowledge_state` 对缺省 runtime 补默认 holder(计划未提,直接构造 AppRuntime 的测试打 /state 不炸);④tests/test_lifespan.py monkeypatch lambda 补 model 参(允许清单外,签名变更必然波及,一行最小改动);⑤rebuild 用例 monkeypatch `kb_admin.REPO_ROOT` 指 tmp(评估集不在场跳过 GT 预检——真实 GT 校验由 T10 validate_corpus 钉);测试语料 DOC_R_SPEC 含 MH-LP100 满足终验 smoke(与真实规格手册同款约定);⑥test_main_boot 按计划 Step 7 注释只断言 title/holder(生产 DDL 校验由 T1 test_db_ch04 覆盖)
- 验收:命令 1 `uv run pytest tests/test_kb_api.py tests/test_schemas.py tests/test_main_boot.py -v` → **40 passed**(kb_api 24 + schemas 13 + main_boot 3);命令 2 全量 `timeout 600 uv run pytest -q -o faulthandler_timeout=240` → **`309 passed, 1 warning in 82.82s`,0 失败**;对账:T10 末 296 → 本任务末 309,净增 13 = kb_api 4 + schemas 6 + main_boot 3 ✓;rebuild 预检走真实 Milvus Lite tempfile 干跑(kb_api 全文件 6.77s,无超时压力);starlette anyio DeprecationWarning 为既有旁观者噪音(T8 已记)
- 翻车与返工:1 次——`_build_production_runtime` 改双参后 create_app 调用点漏传 model,全量首跑 `2 failed, 307 passed`(test_chat_api 生产路径 + test_lifespan monkeypatch 同因),补一行调用点后全绿;TDD 红灯阶段先经 ModuleNotFoundError(app.knowledge.state 缺失)确认

### T12 四策略评估脚本 + 指标纯函数 + faith_cases upsert:done(commit `d5ca8c0`)

(执行期,用户无新输入)

- 完成:`evals/run_retrieval_compare.py` 新建——顶部可 import 纯函数区(无远程副作用,key 检查全在 main):`section_recall_at_k`/`complete_hit_at_k`/`mrr_at_10`(组别名子串覆盖口径,复用 T10 `covered_groups`,k 截断/空组返 0/MRR 逐 rank 单块覆盖判定)、`choose_strategy_threshold`(candidates=全部观测 top1 升序;约束 D 误通过率 ≤ max_d_pass;约束内最大化 pass-adjusted SectionRecall@10,被拒 case recall 计 0;并列取误拒率低再阈值小——key=(par,-over_refusal,-t) 严格大于才替换,升序遍历即先到先得;无可行 SystemExit)、`parse_judge_output`(`\{.*\}` 正则截 JSON,verdict 必须 faithful/fabricated,claims/refs 类型校验,refs 转 int,非法 ValueError)、`upsert_faith_case`(一题一行按 eval_id 查;插入带 first/last_seen;重判更新快照+seen_count+1;已解决复发退回未解决并清空 resolution,resolved_at 不动)。main 九点全量落地:①Settings+embedding 必填/rerank_key 回退检查+`check_ch04_tables`(主库,远程调用前)②`_prepare_eval_db` 复用 ch03 版+临时 Milvus+`run_ingest` 建语料,读回 `{chunk_id:(section_path,answer,scope)}` ③`load_compare_cases`+正例 AND 组语料覆盖断言 ④QueryPlan 冻结(每 case `plan_query` 一次存 dict,四策略共用)⑤四策略×全量 300 `search(strategy=s, min_score=-1.0, query_plan=冻结 plan)` 记 paths 序/top1/effective/note/hits ⑥calibration(每桶奇数)每策略冻结阈值,`_pareto_rows` 全候选表进报告+stdout 摘要 ⑦test(偶数)`_test_metrics`:SR@5/10、CH@5/10、MRR@10(pass 才计,refuse 计 0)、D 拒答正确率、误拒率、by_bucket 聚合;全量 300 diagnosis(逐 case×四策略 top1/passed/recall10)⑧生成段(test,hybrid_rerank):低置信不调模型直接 `REFUSAL_ANSWER`;否则 `assemble_evidence` 组模拟第二轮 messages(System(SERVICE)+Human(query)+AIMessage(tool_calls=query_faq,带 type 字段防 T6 坑)+ToolMessage(query_faq 出参 JSON))同步 invoke;拒答判定=strip 后==REFUSAL_ANSWER;非拒答 A/B/C/E 交 judge(`JUDGE_PROMPT` 占位符替换防花括号,失败重试一次,再失败 judge_error);编造 `upsert_faith_case` 落主库会话 ⑨报告 `{UTC}_compare.json`+`.md` 落盘;硬门槛 judge_error==0 且 bm25 B_model test SR@10 ≥ 0.5,退出码反映;`--max-d-pass`(默认 0.10)/`--help` 解析。`tests/test_eval_metrics.py` 追加计划 Step 1 五例。`tests/test_faith_cases.py` 新建 DB 集成(dbfixtures):插入→人工已解决→复发退回/清空/seen_count=2/快照更新。`evals/run_knowledge_eval.py` 适配:search 改 `strategy="dense"`+`query_plan=passthrough_plan(query)`,hits 取 `res.hits`;docstring 加弃用说明(ch04 语料换血标注失效,仅供指标函数单测复用)
- 偏差(6 处,均有依据):①`choose_strategy_threshold` 内部抽 `_threshold_candidates`/`_score_threshold` 私有 helper,供新加 `_pareto_rows`(报告的 Pareto 候选表)复用同一打分公式——计划只给了选择器本身,「打印 Pareto 候选表」需要全候选枚举,复制公式易漂移;选择循环与计划逐字同义,五例单测钉死不变 ②recall 族宏平均只计 A/B/C/E 正例——D 桶 gt_groups 恒空、recall 恒 0,混入宏平均会系统性拉低且与「D 拒答正确率/误拒率单列」语义重复;B 门槛走 by_bucket 聚合不受影响 ③计划「读回 {chunk_id:(section_path,answer,scope)}」按字面实现,answer/scope 目前只服务于报告语料计数,指标只用 section_path ④diagnosis 落为逐 case×四策略 {top1,passed@冻结阈值,recall10}(300×4 行 JSON),MD 不重复、指向 .json ⑤test_faith_cases 在计划用例外追加 resolved_at 复发保留断言(计划注释明示可选变体,DDL 语义「复发后仍保留」)⑥`_parse_max_d_pass` 顺带支持 `--help`(验收命令 3 二选一之外,打印模块 docstring 后 SystemExit(0))
- 验收:命令 1 `uv run pytest tests/test_eval_metrics.py tests/test_faith_cases.py -v` → **14 passed**(eval_metrics 13 = 旧 8 + 新 5;faith_cases 1);命令 2 全量 `timeout 600 uv run pytest -q -o faulthandler_timeout=240` → **`315 passed, 1 warning in 83.06s`,0 失败**;对账:T11 末 309 → 本任务末 315,净增 6 = eval_metrics 5 + faith_cases 1 ✓;starlette anyio DeprecationWarning 为既有旁观者噪音(T8 已记);命令 3 `uv run python -c 'import evals.run_retrieval_compare'` → import ok(无远程副作用),`--help` 正常打印 docstring。真实运行(需 EMBEDDING_API_KEY)为 T14 验收,本任务未跑
- 翻车与返工:无;两测试文件先红(ModuleNotFoundError: No module named 'evals.run_retrieval_compare')后一轮绿,全量一次通过

### T13 前端(chat.html 引用角标 + 满意度反馈;kb.html 重建 + 策略自测):done(commit `0ffe89b`)

(执行期用户有一次任务输入:vibe coding 例外——不套 TDD/review,先改页面再补字符串断言;浏览器人工验收留给用户)

- 完成:`chat.html`——①citations:`parseEvents` 分发加 `citations` 分支存 `lastCitations`(Done 前到达);回复完成后 `renderCitations` 按 `/\[(\d{1,2})\]/g` 重建子树(textNode + `sup.cite` 逐段拼,**不用 innerHTML**——正文是模型输出;越界编号保持原样,角标 `setAttribute("data-n", n)`);点击角标 `openCitePop` 在 body 上挂 `.cite-pop`(absolute,按 getBoundingClientRect+scrollY 定位,左侧按视口宽度钳制)显示 `lastCitations[n-1]` 的 section_path(mono 头)与 answer(pre-wrap,40vh 限高),点空白(document click 委托,弹层内/角标切换不关)关闭。②反馈:成功回复(非 error 行)在 `.msg-col` 末尾挂 `.feedback`(👍/👎 + 状态文案),点击写 `localStorage["fb:"+sessionId+":"+replyIndex]` 并一次性锁定(selected 高亮 + 双 disabled + 「已反馈」),创建时按 localStorage 恢复锁定态;replyIndex 由 `fbSessionId` 跟踪,session 帧 id 变更即归零(新对话/换会话重计)。样式全部沿用 :root tokens(--accent/--surface-warm 等),零新框架
- 完成:`kb.html`——①建库卡 btn-row 加 `rebuild-btn`(danger,`data-write`,title 提示),confirm 文案含「清空全部知识块(含手工块与挖掘块)及 Milvus 向量」警示,走 `runAction("/kb/api/rebuild", undefined, "重建完成")` 范式;②检索自测卡加 strategy 下拉(四策略,默认 hybrid_rerank)与 scope 下拉(空「(不限)」+六枚举),网格改 `2fr 80px 100px 1fr 1fr`(移动端既有 1fr 媒体查询兜底);scope 空//$ 不下发,留给服务端装配 retriever 自选(strategy 恒下发);③闸条区(gauge)首卡显示 `knowledge_state`(ready 绿/rebuilding 黄/rebuild_required 红,null 显示原值);④`knowledge_state==="rebuilding"` 时全部写按钮禁用:`data-write` 标记六枚(录入/建库/重建/挖掘/向量化/选择性重建;切块预览与差异预览只读不带),`setBusy` 复合 `writeLocked` 状态防 refresh 后 setBusy(false) 误解锁,renderAll 末尾重放。④卡 desc「重建」歧义澄清为「选择性重建」并指回全量重置入口
- 测试:`test_chat_page.py` 追加计划原文 `test_chat_page_citations_and_feedback`(citations 分支/cite-pop/data-n/feedback/已反馈/localStorage/👍👎);`test_kb_page.py` 追加 `test_kb_page_rebuild_and_strategy`(/kb/api/rebuild、rebuild-btn+全量重置+清空警示文案、knowledge_state、strategy 四 option、scope 六 option、search-strategy/search-scope、rebuilding、data-write)
- 偏差(2 处,均为防御性补强):①kb 断言在计划要求(`/kb/api/rebuild`/`knowledge_state`/strategy 下拉项)之上追加清空警示文案、scope 六枚举、data-write/rebuilding 锚点——字符串断言只为钉锚点,多钉几处防静默回退;②角标点击弹层挂 body 而非 `.msg` 内(#messages overflow-y:auto 下 overflow-x 计算值会变 auto,超宽弹层会引发横向滚动条),定位语义仍是 absolute
- 验收:命令 1 `uv run pytest tests/test_chat_page.py tests/test_kb_page.py -v` → **9 passed**(chat_page 5 + kb_page 4,各含新增 1);命令 2 全量 `timeout 600 uv run pytest -q -o faulthandler_timeout=240` → **`317 passed, 1 warning in 83.14s`,0 失败**;对账:T12 末 315 → 本任务末 317,净增 2 = 两页面测试各 1 ✓;starlette anyio DeprecationWarning 为既有旁观者噪音(T8 已记)。浏览器人工验收(问 MH-LP100 猫砂容量→点 [1] 弹层→👍 锁定→刷新仍在→/kb 重建)按任务约定留给用户,T14 演示验收覆盖
- 翻车与返工:无;页面改完一轮测试即绿。写码中自纠两处:openCitePop 初版 Object.assign 链式 appendChild 不可读改平铺,sup 点击回调残留无效判断删除、restore() 无谓包装拍扁为直调 lock()
